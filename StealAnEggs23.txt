
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local IMAGE_MENU_BUTTON = "rbxassetid://121715364276099"
local IMAGE_SEARCH = "rbxassetid://73858257403039"
local IMAGE_MAIN = "rbxassetid://139357095045310"
local IMAGE_AUTOBUY = "rbxassetid://110805385284419"
local IMAGE_FARM = "rbxassetid://102352181267949"
local IMAGE_CONFIG = "rbxassetid://114794077382466"
local IMAGE_CREDITS = "rbxassetid://99243311837005"
local IMAGE_CLICK = "rbxassetid://97594499544606"
local IMAGE_DISCORD_COPIED = "rbxassetid://132744109593099"
local IMAGE_GAME_ID = "rbxassetid://88704800286791"
local DISCORD_LINK = "https://discord.gg/72P6NhC5Pf"

local COLORS = {
	Black = Color3.fromRGB(11, 11, 13),
	Black2 = Color3.fromRGB(16, 16, 18),
	DarkGray = Color3.fromRGB(24, 24, 27),
	Gray = Color3.fromRGB(43, 43, 47),
	Gray2 = Color3.fromRGB(58, 58, 63),
	Gray3 = Color3.fromRGB(73, 73, 79),
	White = Color3.fromRGB(245, 245, 247),
	SoftWhite = Color3.fromRGB(212, 212, 216),
	Muted = Color3.fromRGB(155, 155, 162),
	BlackLine = Color3.fromRGB(4, 4, 5),
}

local function make(className, properties, parent)
	local object = Instance.new(className)
	if className == "TextLabel" or className == "TextButton" or className == "TextBox" then
		pcall(function()
			object.AutoLocalize = false
		end)
	end
	for key, value in pairs(properties or {}) do
		object[key] = value
	end
	if className == "TextLabel" or className == "TextButton" or className == "TextBox" then
		pcall(function() object.AutoLocalize = false end)
	end
	object.Parent = parent
	return object
end

local function applyEnglishOnly(root)
	local function visit(obj)
		pcall(function()
			if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
				obj.AutoLocalize = false
			end
		end)
		for _, child in ipairs(obj:GetChildren()) do
			visit(child)
		end
	end
	visit(root)
end

local function installEnglishGuard(root)
	applyEnglishOnly(root)
	return root.DescendantAdded:Connect(function(obj)
		local _ = pcall(function()
			if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
				obj.AutoLocalize = false
			end
		end)
	end)
end

local function tween(object, time, properties, style, direction)
	local info = TweenInfo.new(time or 0.18, style or Enum.EasingStyle.Quint, direction or Enum.EasingDirection.Out)
	local animation = TweenService:Create(object, info, properties)
	animation:Play()
	return animation
end

local cleanupCallbacks = {}
local cleanedUp = false
local pendingAction = nil

local function registerCleanup(callback)
	if type(callback) ~= "function" then
		return
	end
	table.insert(cleanupCallbacks, callback)
end

local function cleanupAll()
	if cleanedUp then
		return
	end
	cleanedUp = true

	for i = #cleanupCallbacks, 1, -1 do
		local callback = cleanupCallbacks[i]
		cleanupCallbacks[i] = nil
		pcall(callback)
	end
end

local ScreenGui = make("ScreenGui", {
	Name = "PhantomHub_StealAnEgg",
	ResetOnSpawn = false,
	IgnoreGuiInset = true,
	DisplayOrder = 999999,
	ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
}, PlayerGui)

local RootScale = make("UIScale", {Scale = 1}, ScreenGui)

local function getViewport()
	local camera = workspace.CurrentCamera
	if not camera then
		return Vector2.new(1280, 720)
	end
	return camera.ViewportSize
end

local function updateGlobalScale()
	local viewport = getViewport()
	local reference = math.min(viewport.X, viewport.Y)
	local target = reference / 720
	RootScale.Scale = math.clamp(target, 0.82, 1.22)
end

updateGlobalScale()
if workspace.CurrentCamera then
	local viewportSizeConnection = workspace.CurrentCamera:GetPropertyChangedSignal("ViewportSize"):Connect(updateGlobalScale)
	registerCleanup(function()
		viewportSizeConnection:Disconnect()
	end)
end

local LauncherFrame = make("Frame", {
	Name = "Launcher",
	AnchorPoint = Vector2.new(0, 0),
	Position = UDim2.fromScale(0.035, 0.34),
	Size = UDim2.fromScale(0.110, 0.110),
	BackgroundTransparency = 1,
}, ScreenGui)

make("UIAspectRatioConstraint", {
	AspectRatio = 1,
	DominantAxis = Enum.DominantAxis.Width,
}, LauncherFrame)

local Launcher = make("ImageButton", {
	Name = "OpenButton",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.fromScale(1, 1),
	BackgroundColor3 = COLORS.Black,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Image = IMAGE_MENU_BUTTON,
	ImageColor3 = COLORS.White,
	ScaleType = Enum.ScaleType.Crop,
	Active = true,
}, LauncherFrame)

make("UIAspectRatioConstraint", {AspectRatio = 1}, Launcher)

local LauncherStroke = make("UIStroke", {
	Thickness = 2.4,
	Color = COLORS.Gray3,
	Transparency = 0.08,
	ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
}, Launcher)

local launcherHover = false
Launcher.MouseEnter:Connect(function()
	launcherHover = true
	tween(LauncherStroke, 0.12, {Color = COLORS.White, Transparency = 0})
	tween(Launcher, 0.12, {BackgroundColor3 = COLORS.DarkGray})
end)
Launcher.MouseLeave:Connect(function()
	launcherHover = false
	tween(LauncherStroke, 0.12, {Color = COLORS.Gray3, Transparency = 0.08})
	tween(Launcher, 0.12, {BackgroundColor3 = COLORS.Black})
end)

local Menu = make("Frame", {
	Name = "Menu",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.fromScale(0.54, 0.61),
	BackgroundColor3 = COLORS.Black,
	BorderSizePixel = 0,
	Visible = false,
	ClipsDescendants = true,
	Active = true,
}, ScreenGui)

make("UIAspectRatioConstraint", {
	AspectRatio = 1.50,
	DominantAxis = Enum.DominantAxis.Width,
	AspectType = Enum.AspectType.ScaleWithParentSize,
}, Menu)

local MenuStroke = make("UIStroke", {
	Thickness = 2.0,
	Color = COLORS.Gray3,
	Transparency = 0.12,
}, Menu)

local Shadow = make("Frame", {
	Name = "ShadowBorder",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.fromScale(1.016, 1.025),
	BackgroundColor3 = COLORS.BlackLine,
	BackgroundTransparency = 0.38,
	BorderSizePixel = 0,
	ZIndex = 0,
}, Menu)

local Header = make("Frame", {
	Name = "Header",
	Position = UDim2.fromScale(0, 0),
	Size = UDim2.fromScale(1, 0.105),
	BackgroundColor3 = COLORS.Black2,
	BorderSizePixel = 0,
	ZIndex = 3,
}, Menu)

make("UIStroke", {
	Thickness = 1,
	Color = COLORS.Gray,
	Transparency = 0.38,
}, Header)

local HeaderTitle = make("TextLabel", {
	Name = "Title",
	Position = UDim2.fromScale(0.032, 0.10),
	Size = UDim2.fromScale(0.64, 0.82),
	BackgroundTransparency = 1,
	Text = '<font color="#F5F5F7">Phantom Hub | Steal an Eggs</font>\n<font color="#66666B">Mod Menu</font>',
	RichText = true,
	TextColor3 = COLORS.White,
	Font = Enum.Font.GothamBold,
	TextSize = 18,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Center,
	ZIndex = 4,
}, Header)

local LockButton = make("TextButton", {
	Name = "Lock",
	AnchorPoint = Vector2.new(1, 0.5),
	Position = UDim2.fromScale(0.825, 0.5),
	Size = UDim2.fromScale(0.060, 0.70),
	BackgroundColor3 = COLORS.Gray,
	BackgroundTransparency = 0.18,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "Lock",
	TextColor3 = COLORS.White,
	Font = Enum.Font.GothamBold,
	TextSize = 12,
	ZIndex = 20,
}, Header)

local MinimizeButton = make("TextButton", {
	Name = "Minimize",
	AnchorPoint = Vector2.new(1, 0.5),
	Position = UDim2.fromScale(0.895, 0.5),
	Size = UDim2.fromScale(0.050, 0.70),
	BackgroundColor3 = COLORS.Gray,
	BackgroundTransparency = 0.18,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "−",
	TextColor3 = COLORS.White,
	Font = Enum.Font.GothamBold,
	TextSize = 20,
	ZIndex = 20,
}, Header)

local CloseButton = make("TextButton", {
	Name = "Close",
	AnchorPoint = Vector2.new(1, 0.5),
	Position = UDim2.fromScale(0.975, 0.5),
	Size = UDim2.fromScale(0.060, 0.70),
	BackgroundColor3 = COLORS.Gray,
	BackgroundTransparency = 0.18,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Active = true,
	Selectable = false,
	Text = "X",
	TextColor3 = COLORS.White,
	Font = Enum.Font.GothamBold,
	TextSize = 14,
	ZIndex = 50,
}, Header)

local function styleHeaderButton(button)
	button.MouseEnter:Connect(function()
		tween(button, 0.12, {BackgroundColor3 = COLORS.Gray3, BackgroundTransparency = 0})
	end)
	button.MouseLeave:Connect(function()
		tween(button, 0.12, {BackgroundColor3 = COLORS.Gray, BackgroundTransparency = 0.18})
	end)
end
styleHeaderButton(LockButton)
styleHeaderButton(MinimizeButton)
styleHeaderButton(CloseButton)

local Body = make("Frame", {
	Name = "Body",
	Position = UDim2.fromScale(0, 0.105),
	Size = UDim2.fromScale(1, 0.895),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ZIndex = 2,
}, Menu)

local Sidebar = make("Frame", {
	Name = "Sidebar",
	Position = UDim2.fromScale(0, 0),
	Size = UDim2.fromScale(0.235, 1),
	BackgroundColor3 = COLORS.Black2,
	BorderSizePixel = 0,
	ZIndex = 3,
}, Body)
make("UIStroke", {
	Thickness = 1,
	Color = COLORS.Gray,
	Transparency = 0.45,
}, Sidebar)

local Content = make("Frame", {
	Name = "Content",
	Position = UDim2.fromScale(0.235, 0),
	Size = UDim2.fromScale(0.765, 1),
	BackgroundColor3 = COLORS.Black,
	BorderSizePixel = 0,
	ZIndex = 3,
}, Body)

local SearchBar = make("Frame", {
	Name = "SearchBar",
	Position = UDim2.fromScale(0.035, 0.035),
	Size = UDim2.fromScale(0.93, 0.095),
	BackgroundColor3 = COLORS.DarkGray,
	BorderSizePixel = 0,
	ZIndex = 5,
}, Content)
make("UIStroke", {
	Thickness = 1,
	Color = COLORS.Gray2,
	Transparency = 0.18,
}, SearchBar)

local SearchIcon = make("ImageLabel", {
	Name = "Icon",
	Position = UDim2.fromScale(0.035, 0.23),
	Size = UDim2.fromScale(0.075, 0.54),
	BackgroundTransparency = 1,
	Image = IMAGE_SEARCH,
	ImageColor3 = COLORS.SoftWhite,
	ScaleType = Enum.ScaleType.Fit,
	ZIndex = 6,
}, SearchBar)

local SearchBox = make("TextBox", {
	Name = "Input",
	Position = UDim2.fromScale(0.12, 0),
	Size = UDim2.fromScale(0.78, 1),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ClearTextOnFocus = false,
	PlaceholderText = "",
	Text = "",
	TextColor3 = COLORS.White,
	TextTransparency = 0,
	Font = Enum.Font.Gotham,
	TextSize = 15,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Center,
	ZIndex = 6,
}, SearchBar)

local SearchHint = make("TextLabel", {
	Name = "Hint",
	Position = UDim2.fromScale(0.12, 0),
	Size = UDim2.fromScale(0.78, 1),
	BackgroundTransparency = 1,
	Text = "Search...",
	TextColor3 = COLORS.Muted,
	Font = Enum.Font.Gotham,
	TextSize = 15,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Center,
	Visible = true,
	ZIndex = 7,
}, SearchBar)

local SearchClear = make("TextButton", {
	Name = "Clear",
	AnchorPoint = Vector2.new(1, 0.5),
	Position = UDim2.fromScale(0.965, 0.5),
	Size = UDim2.fromScale(0.07, 0.62),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "X",
	TextColor3 = COLORS.SoftWhite,
	Font = Enum.Font.GothamBold,
	TextSize = 13,
	Visible = false,
	ZIndex = 8,
}, SearchBar)


local LockOverlay = make("Frame", {
	Name = "LockOverlay",
	Position = UDim2.fromScale(0, 0),
	Size = UDim2.fromScale(1, 1),
	BackgroundColor3 = COLORS.Black,
	BackgroundTransparency = 0.42,
	BorderSizePixel = 0,
	Visible = false,
	Active = true,
	ZIndex = 1000,
}, ScreenGui)

local LockPanel = make("Frame", {
	Name = "LockPanel",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.fromScale(0.46, 0.40),
	BackgroundColor3 = COLORS.Black2,
	BorderSizePixel = 0,
	Active = true,
	ZIndex = 1001,
}, LockOverlay)
make("UIAspectRatioConstraint", {AspectRatio = 1.65}, LockPanel)
make("UIStroke", {
	Thickness = 1.4,
	Color = COLORS.Gray3,
	Transparency = 0.08,
}, LockPanel)

local LockTitle = make("TextLabel", {
	Position = UDim2.fromScale(0.08, 0.08),
	Size = UDim2.fromScale(0.72, 0.18),
	BackgroundTransparency = 1,
	Text = "Lock Controls",
	TextColor3 = COLORS.White,
	Font = Enum.Font.GothamBold,
	TextSize = 16,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Center,
	ZIndex = 1002,
}, LockPanel)

local LockPanelClose = make("TextButton", {
	Name = "Close",
	AnchorPoint = Vector2.new(1, 0),
	Position = UDim2.fromScale(0.94, 0.05),
	Size = UDim2.fromScale(0.060, 0.70),
	BackgroundColor3 = COLORS.Gray,
	BackgroundTransparency = 0.18,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Active = true,
	Selectable = false,
	Text = "X",
	TextColor3 = COLORS.White,
	Font = Enum.Font.GothamBold,
	TextSize = 13,
	ZIndex = 1003,
}, LockPanel)
make("UIAspectRatioConstraint", {AspectRatio = 1}, LockPanelClose)

local LockDescription = make("TextLabel", {
	Position = UDim2.fromScale(0.08, 0.25),
	Size = UDim2.fromScale(0.84, 0.20),
	BackgroundTransparency = 1,
	Text = "Lock the mod menu and/or its button in their current position.",
	TextColor3 = COLORS.Muted,
	Font = Enum.Font.Gotham,
	TextSize = 12,
	TextWrapped = true,
	TextXAlignment = Enum.TextXAlignment.Center,
	TextYAlignment = Enum.TextYAlignment.Center,
	ZIndex = 1002,
}, LockPanel)

local lockMenuSelected = false
local lockButtonSelected = false
local lockEnabled = false

local function createLockChoice(name, text, x)
	local Button = make("TextButton", {
		Name = name,
		Position = UDim2.fromScale(x, 0.48),
		Size = UDim2.fromScale(0.38, 0.18),
		BackgroundColor3 = COLORS.Black,
		BorderSizePixel = 0,
		AutoButtonColor = false,
		Text = "",
		Active = true,
		ZIndex = 1003,
	}, LockPanel)
	make("UIStroke", {Thickness = 1.2, Color = COLORS.Gray3, Transparency = 0.05}, Button)

	local Label = make("TextLabel", {
		Position = UDim2.fromScale(0.08, 0),
		Size = UDim2.fromScale(0.76, 1),
		BackgroundTransparency = 1,
		Text = text,
		TextColor3 = COLORS.White,
		Font = Enum.Font.GothamSemibold,
		TextSize = 12,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 1004,
	}, Button)

	local Check = make("TextLabel", {
		AnchorPoint = Vector2.new(1, 0.5),
		Position = UDim2.fromScale(0.93, 0.5),
		Size = UDim2.fromScale(0.18, 0.70),
		BackgroundTransparency = 1,
		Text = "",
		TextColor3 = COLORS.White,
		Font = Enum.Font.GothamBold,
		TextSize = 14,
		TextXAlignment = Enum.TextXAlignment.Center,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 1004,
	}, Button)

	local state = false
	local function setState(value)
		state = value == true
		Button.BackgroundColor3 = state and COLORS.Gray2 or COLORS.Black
		Check.Text = state and "✓" or ""
	end

	Button.Activated:Connect(function()
		setState(not state)
		if name == "MenuChoice" then
			lockMenuSelected = state
		else
			lockButtonSelected = state
		end
	end)
	setState(false)
	return Button
end

createLockChoice("MenuChoice", "Lock Menu", 0.08)
createLockChoice("ButtonChoice", "Lock Button", 0.54)

local LockEnableToggle = make("TextButton", {
	Name = "EnableLock",
	AnchorPoint = Vector2.new(0.5, 0),
	Position = UDim2.fromScale(0.5, 0.72),
	Size = UDim2.fromScale(0.13, 0.10),
	BackgroundColor3 = COLORS.Gray,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "",
	ZIndex = 1003,
}, LockPanel)
make("UICorner", {CornerRadius = UDim.new(1, 0)}, LockEnableToggle)
local LockEnableKnob = make("Frame", {
	Name = "Knob",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.23, 0.5),
	Size = UDim2.fromScale(0.34, 0.78),
	BackgroundColor3 = COLORS.White,
	BorderSizePixel = 0,
	ZIndex = 1004,
}, LockEnableToggle)
make("UICorner", {CornerRadius = UDim.new(1, 0)}, LockEnableKnob)
local LockEnableLabel = make("TextLabel", {
	AnchorPoint = Vector2.new(0.5, 0),
	Position = UDim2.fromScale(0.5, 0.84),
	Size = UDim2.fromScale(0.70, 0.10),
	BackgroundTransparency = 1,
	Text = "Enable Lock",
	TextColor3 = COLORS.White,
	Font = Enum.Font.GothamSemibold,
	TextSize = 12,
	TextXAlignment = Enum.TextXAlignment.Center,
	TextYAlignment = Enum.TextYAlignment.Center,
	ZIndex = 1004,
}, LockPanel)

local function updateLockToggle()
	local targetColor = lockEnabled and COLORS.Gray3 or COLORS.Gray
	local targetPosition = UDim2.fromScale(lockEnabled and 0.77 or 0.23, 0.5)
	tween(LockEnableToggle, 0.16, {BackgroundColor3 = targetColor})
	tween(LockEnableKnob, 0.16, {Position = targetPosition})
end

local function setLockEnabled(value)
	lockEnabled = value == true
	updateLockToggle()
end

updateLockToggle()

LockEnableToggle.Activated:Connect(function()
	setLockEnabled(not lockEnabled)
end)

local function closeLockPanel()
	LockOverlay.Visible = false
end

LockPanelClose.MouseEnter:Connect(function()
	tween(LockPanelClose, 0.10, {BackgroundColor3 = COLORS.Gray3, BackgroundTransparency = 0})
end)
LockPanelClose.MouseLeave:Connect(function()
	tween(LockPanelClose, 0.10, {BackgroundColor3 = COLORS.Gray, BackgroundTransparency = 0.18})
end)
LockPanelClose.Activated:Connect(closeLockPanel)

local function openLockPanel()
	if cleanedUp or not ScreenGui.Parent then
		return
	end
	LockOverlay.Visible = true
	LockPanel.Position = UDim2.fromScale(0.5, 0.5)
end

LockButton.Activated:Connect(openLockPanel)

local TabList = make("ScrollingFrame", {
	Name = "Tabs",
	Position = UDim2.fromScale(0.055, 0.045),
	Size = UDim2.fromScale(0.89, 0.90),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ScrollBarThickness = 3,
	ScrollBarImageColor3 = COLORS.White,
	ScrollBarImageTransparency = 0.05,
	CanvasSize = UDim2.fromScale(0, 1.08),
	AutomaticCanvasSize = Enum.AutomaticSize.Y,
	ScrollingDirection = Enum.ScrollingDirection.Y,
	ScrollingEnabled = true,
	Active = true,
	ZIndex = 4,
}, Sidebar)

make("UIListLayout", {
	Padding = UDim.new(0, 0),
	SortOrder = Enum.SortOrder.LayoutOrder,
	HorizontalAlignment = Enum.HorizontalAlignment.Center,
}, TabList)

-- Compact local player information shown at the bottom-left of the Sidebar.
-- The tab list keeps its original size and position.
local UserInfo = make("Frame", {
	Name = "UserInfo",
	AnchorPoint = Vector2.new(0, 1),
	Position = UDim2.new(0.055, 0, 1, -5),
	Size = UDim2.new(0.89, 0, 0, 34),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ZIndex = 8,
}, Sidebar)

local UserAvatar = make("ImageLabel", {
	Name = "Avatar",
	Position = UDim2.new(0, 0, 0.5, -15),
	Size = UDim2.fromOffset(30, 30),
	BackgroundColor3 = COLORS.DarkGray,
	BackgroundTransparency = 0,
	BorderSizePixel = 0,
	Image = "",
	ScaleType = Enum.ScaleType.Crop,
	ZIndex = 9,
}, UserInfo)
make("UICorner", {
	CornerRadius = UDim.new(1, 0),
}, UserAvatar)
make("UIStroke", {
	Thickness = 1.2,
	Color = COLORS.Gray3,
	Transparency = 0.08,
}, UserAvatar)

local UserNameLabel = make("TextLabel", {
	Name = "Username",
	Position = UDim2.fromOffset(38, 1),
	Size = UDim2.new(1, -38, 0, 16),
	BackgroundTransparency = 1,
	Text = LocalPlayer.Name,
	TextColor3 = COLORS.White,
	TextTransparency = 0,
	Font = Enum.Font.GothamBold,
	TextSize = 11,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Center,
	TextTruncate = Enum.TextTruncate.AtEnd,
	ZIndex = 9,
}, UserInfo)
make("UIStroke", {
	Thickness = 0.7,
	Color = COLORS.White,
	Transparency = 0.78,
}, UserNameLabel)

local DisplayNameLabel = make("TextLabel", {
	Name = "DisplayName",
	Position = UDim2.fromOffset(38, 16),
	Size = UDim2.new(1, -38, 0, 15),
	BackgroundTransparency = 1,
	Text = "",
	TextColor3 = COLORS.Muted,
	TextTransparency = 0,
	Font = Enum.Font.Gotham,
	TextSize = 9,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Center,
	TextTruncate = Enum.TextTruncate.AtEnd,
	Visible = false,
	ZIndex = 9,
}, UserInfo)

if LocalPlayer.DisplayName ~= LocalPlayer.Name then
	DisplayNameLabel.Text = "(@" .. LocalPlayer.DisplayName .. ")"
	DisplayNameLabel.Visible = true
end

task.spawn(function()
	local success, content = pcall(function()
		return Players:GetUserThumbnailAsync(
			LocalPlayer.UserId,
			Enum.ThumbnailType.HeadShot,
			Enum.ThumbnailSize.Size100x100
		)
	end)
	if success and content and content ~= "" and UserAvatar.Parent then
		UserAvatar.Image = content
	end
end)

local tabs = {
	{Name = "Main", Icon = IMAGE_MAIN},
	{Name = "Auto Buy", Icon = IMAGE_AUTOBUY},
	{Name = "Farm", Icon = IMAGE_FARM},
	{Name = "Config", Icon = IMAGE_CONFIG},
	{Name = "Credits", Icon = IMAGE_CREDITS},
}

local tabButtons = {}
local currentTab = "Main"
local visualPanelOpen = false

local ResultsScroll = make("ScrollingFrame", {
	Name = "Results",
	Position = UDim2.fromScale(0.035, 0.155),
	Size = UDim2.fromScale(0.93, 0.815),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ScrollBarThickness = 3,
	ScrollBarImageColor3 = COLORS.White,
	ScrollBarImageTransparency = 0.05,
	CanvasSize = UDim2.fromScale(1, 0),
	AutomaticCanvasSize = Enum.AutomaticSize.Y,
	ScrollingDirection = Enum.ScrollingDirection.Y,
	ScrollingEnabled = true,
	Active = true,
	Selectable = false,
	ElasticBehavior = Enum.ElasticBehavior.WhenScrollable,
	ZIndex = 4,
}, Content)

make("UIListLayout", {
	Padding = UDim.new(0, 7),
	SortOrder = Enum.SortOrder.LayoutOrder,
	HorizontalAlignment = Enum.HorizontalAlignment.Center,
}, ResultsScroll)

local ResultsPadding = make("UIPadding", {
	PaddingTop = UDim.new(0, 2),
	PaddingBottom = UDim.new(0, 8),
	PaddingRight = UDim.new(0, 40),
}, ResultsScroll)

local resultsScrollWheelConnection
local resultsScrollTouchStart = nil
local resultsScrollTouchCanvasY = nil
local resultsScrollInputConnection

local function isResultsScrollingAllowed()
	return currentTab ~= "Credits" and ResultsScroll.Visible and ResultsScroll.ScrollingEnabled and not visualPanelOpen
end

local function getResultsMaxScrollY()
	return math.max(0, ResultsScroll.AbsoluteCanvasSize.Y - ResultsScroll.AbsoluteSize.Y)
end

local function setResultsScrollY(y)
	local maxY = getResultsMaxScrollY()
	ResultsScroll.CanvasPosition = Vector2.new(0, math.clamp(y or 0, 0, maxY))
end

local function isPointInsideResults(point)
	if not ResultsScroll.Visible then
		return false
	end
	local position = ResultsScroll.AbsolutePosition
	local size = ResultsScroll.AbsoluteSize
	return point.X >= position.X
		and point.X <= position.X + size.X
		and point.Y >= position.Y
		and point.Y <= position.Y + size.Y
end

local function updateResultsScrollMode()
	local enabled = currentTab ~= "Credits" and not visualPanelOpen
	ResultsScroll.ScrollingEnabled = enabled
	ResultsScroll.Active = enabled
	ResultsScroll.ScrollBarThickness = enabled and 3 or 0
	ResultsScroll.CanvasPosition = Vector2.new(0, 0)
end

local function updateTabListScrollMode()
	local enabled = not visualPanelOpen
	TabList.ScrollingEnabled = enabled
	TabList.Active = enabled
	TabList.ScrollBarThickness = enabled and 3 or 0
	TabList.CanvasPosition = Vector2.new(0, 0)
end

resultsScrollWheelConnection = ResultsScroll.InputChanged:Connect(function(input)
	if not isResultsScrollingAllowed() then
		return
	end

	if input.UserInputType == Enum.UserInputType.MouseWheel then
		local delta = input.Position.Z
		if delta ~= 0 then
			setResultsScrollY(ResultsScroll.CanvasPosition.Y - (delta * ResultsScroll.AbsoluteSize.Y * 0.16))
		end
	elseif input.UserInputType == Enum.UserInputType.Touch then
		resultsScrollTouchStart = input.Position
		resultsScrollTouchCanvasY = ResultsScroll.CanvasPosition.Y
	end
end)

resultsScrollInputConnection = UserInputService.InputChanged:Connect(function(input)
	if not isResultsScrollingAllowed() then
		return
	end

	if input.UserInputType == Enum.UserInputType.Touch and resultsScrollTouchStart and resultsScrollTouchCanvasY then
		local deltaY = input.Position.Y - resultsScrollTouchStart.Y
		if math.abs(deltaY) >= 1 then
			setResultsScrollY(resultsScrollTouchCanvasY - deltaY)
		end
	elseif input.UserInputType == Enum.UserInputType.MouseWheel and isPointInsideResults(input.Position) then
		local delta = input.Position.Z
		if delta ~= 0 then
			setResultsScrollY(ResultsScroll.CanvasPosition.Y - (delta * ResultsScroll.AbsoluteSize.Y * 0.16))
		end
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.Touch then
		resultsScrollTouchStart = nil
		resultsScrollTouchCanvasY = nil
	end
end)

local NotificationContainer = make("Frame", {
	Name = "NotificationContainer",
	AnchorPoint = Vector2.new(1, 1),
	Position = UDim2.fromScale(0.98, 0.96),
	Size = UDim2.fromScale(0.30, 0.42),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	Active = false,
	ZIndex = 2000,
}, ScreenGui)

make("UIListLayout", {
	Padding = UDim.new(0.012, 0),
	HorizontalAlignment = Enum.HorizontalAlignment.Right,
	VerticalAlignment = Enum.VerticalAlignment.Bottom,
	SortOrder = Enum.SortOrder.LayoutOrder,
}, NotificationContainer)

local notificationSerial = 0

local function copyDiscordLink()
	if typeof(setclipboard) == "function" then
		local ok = pcall(setclipboard, DISCORD_LINK)
		if ok then
			return true
		end
	end
	if typeof(toclipboard) == "function" then
		local ok = pcall(toclipboard, DISCORD_LINK)
		if ok then
			return true
		end
	end
	return false
end

local function copyText(text)
	if typeof(setclipboard) == "function" then
		local ok = pcall(setclipboard, text)
		if ok then return true end
	end
	if typeof(toclipboard) == "function" then
		local ok = pcall(toclipboard, text)
		if ok then return true end
	end
	return false
end

local function copyGameId()
	return copyText(tostring(game.PlaceId))
end

local function copyServerId()
	return copyText(tostring(game.JobId))
end

local function requestServerRejoin()
	local success, err = pcall(function()
		TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
	end)
	if not success then
		warn("Phantom Hub Rejoin error:", err)
	end
end

local function showCopiedNotification(message, iconImage)
	if cleanedUp or not ScreenGui.Parent then
		return
	end

	notificationSerial += 1
	local serial = notificationSerial

	local Notice = make("Frame", {
		Name = "CopiedNotification",
		AnchorPoint = Vector2.new(1, 0),
		Size = UDim2.fromScale(1, 0.20),
		BackgroundColor3 = COLORS.Black2,
		BackgroundTransparency = 0.04,
		BorderSizePixel = 0,
		ClipsDescendants = true,
		LayoutOrder = -serial,
		ZIndex = 2001,
	}, NotificationContainer)
	make("UIAspectRatioConstraint", {
		AspectRatio = 4.9,
		DominantAxis = Enum.DominantAxis.Width,
	}, Notice)
	make("UIStroke", {
		Thickness = 1.2,
		Color = COLORS.Gray3,
		Transparency = 0.10,
	}, Notice)

	local Icon = make("ImageLabel", {
		Name = "Icon",
		Position = UDim2.fromScale(0.045, 0.18),
		Size = UDim2.fromScale(0.16, 0.64),
		BackgroundTransparency = 1,
		Image = iconImage or IMAGE_DISCORD_COPIED,
		ImageColor3 = COLORS.White,
		ScaleType = Enum.ScaleType.Fit,
		ZIndex = 2002,
	}, Notice)

	local Label = make("TextLabel", {
		Name = "Label",
		Position = UDim2.fromScale(0.25, 0.08),
		Size = UDim2.fromScale(0.68, 0.78),
		BackgroundTransparency = 1,
		Text = message or "Discord link copied!",
		TextColor3 = COLORS.White,
		Font = Enum.Font.GothamSemibold,
		TextSize = 12,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 2002,
	}, Notice)

	local Progress = make("Frame", {
		Name = "Progress",
		AnchorPoint = Vector2.new(0, 1),
		Position = UDim2.fromScale(0, 1),
		Size = UDim2.fromScale(1, 0.055),
		BackgroundColor3 = COLORS.White,
		BorderSizePixel = 0,
		ZIndex = 2003,
	}, Notice)

	Notice.Position = UDim2.fromScale(1.06, 0)
	Progress.Size = UDim2.fromScale(0, 0.055)

	tween(Notice, 0.24, {
		Position = UDim2.fromScale(1, 0),
	})
	tween(Progress, 1.8, {
		Size = UDim2.fromScale(1, 0.055),
	}, Enum.EasingStyle.Linear)

	task.delay(1.82, function()
		if not Notice.Parent or cleanedUp or serial > notificationSerial then
			if Notice.Parent then
				Notice:Destroy()
			end
			return
		end
		tween(Notice, 0.22, {
			Position = UDim2.fromScale(1.06, 0),
		})
		task.delay(0.24, function()
			if Notice.Parent then
				Notice:Destroy()
			end
		end)
	end)
end

local openConfirm

local function requestServerHop()
	local success, err = pcall(function()
		TeleportService:Teleport(game.PlaceId, LocalPlayer)
	end)
	if not success then
		warn("Phantom Hub Auto Rejoin error:", err)
	end
end

local visualSelectionState = {
	Name = false,
	Mutation = false,
	Distance = false,
	Rarity = false,
}

-- Forward reference used by the Visual selector so changing the selected egg
-- information immediately refreshes the currently enabled Egg ESP.
local refreshEggESP = nil
local VisualEggInfoModal
local VisualEggInfoOverlay
local VisualEggInfoPanel

-- Optional single-rarity filter used by Egg ESP. When nil, Egg ESP behaves normally
-- and labels are created for every egg matching the selected information fields.
local selectedEggRarity = nil
local RarityFilterModal
local RarityFilterOverlay
local RarityFilterPanel

local function setVisualSelection(name, value)
	visualSelectionState[name] = value == true
	if refreshEggESP then
		pcall(refreshEggESP)
	end
end

local function updateVisualSelectionButton(button, knob, name)
	local on = visualSelectionState[name] == true
	local targetColor = on and COLORS.Gray3 or COLORS.Gray
	local targetPosition = UDim2.fromScale(on and 0.77 or 0.23, 0.5)
	tween(button, 0.14, {BackgroundColor3 = targetColor})
	tween(knob, 0.14, {Position = targetPosition})
end

local function createVisualSelectionRow(parent, name, text, order)
	local Row = make("Frame", {
		Name = name .. "Row",
		Size = UDim2.new(1, -18, 0, 62),
		BackgroundColor3 = COLORS.Black2,
		BorderSizePixel = 0,
		LayoutOrder = order,
		ZIndex = 1004,
	}, parent)
	make("UIStroke", {Thickness = 1.15, Color = COLORS.Gray3, Transparency = 0.16, ApplyStrokeMode = Enum.ApplyStrokeMode.Border}, Row)

	make("TextLabel", {
		Position = UDim2.new(0, 14, 0, 0),
		Size = UDim2.new(1, -90, 1, 0),
		BackgroundTransparency = 1,
		Text = text,
		TextColor3 = COLORS.White,
		Font = Enum.Font.GothamSemibold,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 1005,
	}, Row)

	local ToggleButton = make("TextButton", {
		Name = "Toggle",
		AnchorPoint = Vector2.new(1, 0.5),
		Position = UDim2.new(1, -14, 0.5, 0),
		Size = UDim2.fromOffset(52, 24),
		BackgroundColor3 = COLORS.Gray,
		BorderSizePixel = 0,
		AutoButtonColor = false,
		Text = "",
		ZIndex = 1006,
	}, Row)
	make("UICorner", {CornerRadius = UDim.new(1, 0)}, ToggleButton)

	local Knob = make("Frame", {
		Name = "Knob",
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.23, 0.5),
		Size = UDim2.fromOffset(19, 19),
		BackgroundColor3 = COLORS.White,
		BorderSizePixel = 0,
		ZIndex = 1007,
	}, ToggleButton)
	make("UICorner", {CornerRadius = UDim.new(1, 0)}, Knob)

	updateVisualSelectionButton(ToggleButton, Knob, name)
	ToggleButton.Activated:Connect(function()
		setVisualSelection(name, not visualSelectionState[name])
		updateVisualSelectionButton(ToggleButton, Knob, name)
	end)
	return Row
end

local function closeVisualEggInfoPanel()
	visualPanelOpen = false
	if VisualEggInfoOverlay then
		VisualEggInfoOverlay.Visible = false
	end
	if VisualEggInfoModal then
		VisualEggInfoModal.Visible = false
	end
	updateResultsScrollMode()
	updateTabListScrollMode()
end

local function openVisualEggInfoPanel()
	if cleanedUp or not ScreenGui.Parent then
		return
	end
	if not VisualEggInfoOverlay then
		-- Full-screen input blocker. The Visual settings panel remains interactive
		-- above it, while the menu and launcher underneath become untouchable.
		VisualEggInfoModal = make("Frame", {
			Name = "VisualEggInfoModal",
			Position = UDim2.fromScale(0, 0),
			Size = UDim2.fromScale(1, 1),
			BackgroundColor3 = COLORS.Black,
			BackgroundTransparency = 1,
			BorderSizePixel = 0,
			Visible = false,
			Active = false,
			ZIndex = 1000,
		}, ScreenGui)

		-- Keep the existing panel width and bottom edge; extend only upward
		-- so its top covers the search-bar area.
		VisualEggInfoOverlay = make("Frame", {
			Name = "VisualEggInfoOverlay",
			Position = UDim2.fromScale(0.50, 0.035),
			Size = UDim2.fromScale(0.465, 0.935),
			BackgroundColor3 = COLORS.Black,
			BackgroundTransparency = 0.10,
			BorderSizePixel = 0,
			Visible = false,
			Active = true,
			ClipsDescendants = true,
			ZIndex = 100,
		}, Content)
		make("UIStroke", {Thickness = 1.4, Color = COLORS.Gray3, Transparency = 0.05}, VisualEggInfoOverlay)

		VisualEggInfoPanel = make("Frame", {
			Name = "VisualEggInfoPanel",
			Position = UDim2.fromScale(0.025, 0.020),
			Size = UDim2.fromScale(0.95, 0.96),
			BackgroundColor3 = COLORS.Black2,
			BorderSizePixel = 0,
			Active = true,
			ClipsDescendants = true,
			ZIndex = 101,
		}, VisualEggInfoOverlay)
		make("UIStroke", {Thickness = 1.2, Color = COLORS.Gray2, Transparency = 0.10}, VisualEggInfoPanel)

		make("TextLabel", {
			Position = UDim2.fromScale(0.055, 0.035),
			Size = UDim2.fromScale(0.69, 0.11),
			BackgroundTransparency = 1,
			Text = "Egg ESP Settings",
			TextColor3 = COLORS.White,
			Font = Enum.Font.GothamBold,
			TextSize = 15,
			TextXAlignment = Enum.TextXAlignment.Left,
			TextYAlignment = Enum.TextYAlignment.Center,
			ZIndex = 102,
		}, VisualEggInfoPanel)

		local Close = make("TextButton", {
			Name = "Close",
			AnchorPoint = Vector2.new(1, 0),
			Position = UDim2.fromScale(0.955, 0.035),
			Size = UDim2.fromScale(0.095, 0.105),
			BackgroundColor3 = COLORS.Gray,
			BackgroundTransparency = 0.18,
			BorderSizePixel = 0,
			AutoButtonColor = false,
			Text = "X",
			TextColor3 = COLORS.White,
			Font = Enum.Font.GothamBold,
			TextSize = 13,
			ZIndex = 105,
		}, VisualEggInfoPanel)
		make("UIAspectRatioConstraint", {AspectRatio = 1}, Close)
		Close.MouseEnter:Connect(function() tween(Close, 0.10, {BackgroundColor3 = COLORS.Gray3, BackgroundTransparency = 0}) end)
		Close.MouseLeave:Connect(function() tween(Close, 0.10, {BackgroundColor3 = COLORS.Gray, BackgroundTransparency = 0.18}) end)
		Close.Activated:Connect(closeVisualEggInfoPanel)

		local Scroll = make("ScrollingFrame", {
			Name = "Options",
			Position = UDim2.fromScale(0.045, 0.17),
			Size = UDim2.fromScale(0.91, 0.77),
			BackgroundTransparency = 1,
			BorderSizePixel = 0,
			ScrollBarThickness = 3,
			ScrollBarImageColor3 = COLORS.White,
			ScrollBarImageTransparency = 0.05,
			ScrollingDirection = Enum.ScrollingDirection.Y,
			ScrollingEnabled = true,
			Active = true,
			AutomaticCanvasSize = Enum.AutomaticSize.Y,
			CanvasSize = UDim2.fromScale(0, 1.2),
			ElasticBehavior = Enum.ElasticBehavior.WhenScrollable,
			ZIndex = 103,
		}, VisualEggInfoPanel)
		make("UIListLayout", {Padding = UDim.new(0, 7), SortOrder = Enum.SortOrder.LayoutOrder}, Scroll)
		make("UIPadding", {
			PaddingBottom = UDim.new(0, 10),
			PaddingLeft = UDim.new(0, 25),
			PaddingRight = UDim.new(0, 0),
			PaddingTop = UDim.new(0, 15),
		}, Scroll)
		createVisualSelectionRow(Scroll, "Name", "Name", 1)
		createVisualSelectionRow(Scroll, "Mutation", "Mutation", 2)
		createVisualSelectionRow(Scroll, "Distance", "Distance", 3)
		createVisualSelectionRow(Scroll, "Rarity", "Rarity", 4)
		installEnglishGuard(VisualEggInfoOverlay)
		registerCleanup(function()
			if VisualEggInfoOverlay and VisualEggInfoOverlay.Parent then
				VisualEggInfoOverlay:Destroy()
			end
		end)
	end
	visualPanelOpen = true
	updateResultsScrollMode()
	updateTabListScrollMode()
	VisualEggInfoModal.Visible = true
	VisualEggInfoOverlay.Visible = true
	-- The modal blocks the menu and launcher visually/input-wise while leaving
	-- the Visual settings panel itself interactive until its X button is used.
end

local function normalizeRarityName(value)
	if type(value) ~= "string" then
		return ""
	end
	return string.lower((value:gsub("^%s+", ""):gsub("%s+$", "")))
end

local function getRaritySortValue(rarity)
	if type(rarity) ~= "table" then
		return math.huge
	end

	local numericCandidates = {
		rarity.SortOrder,
		rarity.Order,
		rarity.Priority,
		rarity.Index,
		rarity.Tier,
	}
	for _, value in ipairs(numericCandidates) do
		if type(value) == "number" then
			return value
		end
	end

	local name = normalizeRarityName(rarity.DisplayName or rarity.Name or rarity.Title)
	local knownOrder = {
		["common"] = 1,
		["uncommon"] = 2,
		["rare"] = 3,
		["epic"] = 4,
		["legendary"] = 5,
		["mythic"] = 6,
		["ultra"] = 7,
		["divine"] = 8,
		["special"] = 9,
		["godly"] = 10,
		["brainrot god"] = 11,
		["secret"] = 12,
	}
	return knownOrder[name] or math.huge
end

local function getAvailableEggRarities()
	local shared = game:GetService("ReplicatedStorage")
	local assetsModule = shared:FindFirstChild("Data") and shared.Data:FindFirstChild("Assets")
	local assets = nil
	if assetsModule then
		pcall(function()
			assets = require(assetsModule)
		end)
	end
	local entries = {}
	local seen = {}

	if assets and type(assets) == "table" and type(assets.Directory) == "table" then
		for _, asset in pairs(assets.Directory) do
			if type(asset) == "table" and type(asset.Rarity) == "table" then
				local rarity = asset.Rarity
				local rarityName = rarity.DisplayName or rarity.Name or rarity.Title
				if type(rarityName) == "string" and rarityName ~= "" then
					local key = normalizeRarityName(rarityName)
					if not seen[key] then
						seen[key] = true
						table.insert(entries, {
							Name = rarityName,
							Sort = getRaritySortValue(rarity),
						})
					end
				end
			end
		end
	end

	-- Fallback list keeps the selector usable if the game's asset directory cannot
	-- be read at runtime. These are ordered from lower to higher rarity.
	if #entries == 0 then
		local fallback = {
			"Common",
			"Uncommon",
			"Rare",
			"Epic",
			"Legendary",
			"Mythic",
			"Divine",
			"Secret",
		}
		for index, rarityName in ipairs(fallback) do
			table.insert(entries, {Name = rarityName, Sort = index})
		end
	end

	table.sort(entries, function(a, b)
		if a.Sort ~= b.Sort then
			return a.Sort < b.Sort
		end
		return string.lower(a.Name) < string.lower(b.Name)
	end)

	return entries
end

local function closeRarityFilterPanel()
	visualPanelOpen = false
	if RarityFilterOverlay then
		RarityFilterOverlay.Visible = false
	end
	if RarityFilterModal then
		RarityFilterModal.Visible = false
	end
	updateResultsScrollMode()
	updateTabListScrollMode()
end

local function setSelectedEggRarity(rarityName)
	if type(rarityName) ~= "string" or rarityName == "" then
		selectedEggRarity = nil
	else
		selectedEggRarity = rarityName
	end
	if refreshEggESP then
		pcall(refreshEggESP)
	end
end

local function openRarityFilterPanel()
	if cleanedUp or not ScreenGui.Parent then
		return
	end
	if not RarityFilterOverlay then
		RarityFilterModal = make("Frame", {
			Name = "RarityFilterModal",
			Position = UDim2.fromScale(0, 0),
			Size = UDim2.fromScale(1, 1),
			BackgroundColor3 = COLORS.Black,
			BackgroundTransparency = 1,
			BorderSizePixel = 0,
			Visible = false,
			Active = false,
			ZIndex = 1000,
		}, ScreenGui)

		RarityFilterOverlay = make("Frame", {
			Name = "RarityFilterOverlay",
			Position = UDim2.fromScale(0.50, 0.035),
			Size = UDim2.fromScale(0.465, 0.935),
			BackgroundColor3 = COLORS.Black,
			BackgroundTransparency = 0.10,
			BorderSizePixel = 0,
			Visible = false,
			Active = true,
			ClipsDescendants = true,
			ZIndex = 100,
		}, Content)
		make("UIStroke", {Thickness = 1.4, Color = COLORS.Gray3, Transparency = 0.05}, RarityFilterOverlay)

		RarityFilterPanel = make("Frame", {
			Name = "RarityFilterPanel",
			Position = UDim2.fromScale(0.025, 0.020),
			Size = UDim2.fromScale(0.95, 0.96),
			BackgroundColor3 = COLORS.Black2,
			BorderSizePixel = 0,
			Active = true,
			ClipsDescendants = true,
			ZIndex = 101,
		}, RarityFilterOverlay)
		make("UIStroke", {Thickness = 1.2, Color = COLORS.Gray2, Transparency = 0.10}, RarityFilterPanel)

		make("TextLabel", {
			Position = UDim2.fromScale(0.055, 0.035),
			Size = UDim2.fromScale(0.69, 0.11),
			BackgroundTransparency = 1,
			Text = "Egg Rarity Filter",
			TextColor3 = COLORS.White,
			Font = Enum.Font.GothamBold,
			TextSize = 15,
			TextXAlignment = Enum.TextXAlignment.Left,
			TextYAlignment = Enum.TextYAlignment.Center,
			ZIndex = 102,
		}, RarityFilterPanel)

		local Close = make("TextButton", {
			Name = "Close",
			AnchorPoint = Vector2.new(1, 0),
			Position = UDim2.fromScale(0.955, 0.035),
			Size = UDim2.fromScale(0.095, 0.105),
			BackgroundColor3 = COLORS.Gray,
			BackgroundTransparency = 0.18,
			BorderSizePixel = 0,
			AutoButtonColor = false,
			Text = "X",
			TextColor3 = COLORS.White,
			Font = Enum.Font.GothamBold,
			TextSize = 13,
			ZIndex = 105,
		}, RarityFilterPanel)
		make("UIAspectRatioConstraint", {AspectRatio = 1}, Close)
		Close.MouseEnter:Connect(function() tween(Close, 0.10, {BackgroundColor3 = COLORS.Gray3, BackgroundTransparency = 0}) end)
		Close.MouseLeave:Connect(function() tween(Close, 0.10, {BackgroundColor3 = COLORS.Gray, BackgroundTransparency = 0.18}) end)
		Close.Activated:Connect(closeRarityFilterPanel)

		make("TextLabel", {
			Position = UDim2.fromScale(0.055, 0.115),
			Size = UDim2.fromScale(0.88, 0.075),
			BackgroundTransparency = 1,
			Text = "Select one rarity. Leave it clear to show all eggs.",
			TextColor3 = COLORS.Muted,
			Font = Enum.Font.Gotham,
			TextSize = 11,
			TextWrapped = true,
			TextXAlignment = Enum.TextXAlignment.Left,
			TextYAlignment = Enum.TextYAlignment.Center,
			ZIndex = 102,
		}, RarityFilterPanel)

		local Clear = make("TextButton", {
			Name = "ClearFilter",
			Position = UDim2.fromScale(0.055, 0.195),
			Size = UDim2.fromScale(0.89, 0.075),
			BackgroundColor3 = COLORS.Gray,
			BackgroundTransparency = 0.18,
			BorderSizePixel = 0,
			AutoButtonColor = false,
			Text = "No Filter",
			TextColor3 = COLORS.White,
			Font = Enum.Font.GothamSemibold,
			TextSize = 11,
			ZIndex = 105,
		}, RarityFilterPanel)
		make("UICorner", {CornerRadius = UDim.new(0, 8)}, Clear)

		local List = make("ScrollingFrame", {
			Name = "Rarities",
			Position = UDim2.fromScale(0.045, 0.285),
			Size = UDim2.fromScale(0.91, 0.625),
			BackgroundTransparency = 1,
			BorderSizePixel = 0,
			ScrollBarThickness = 3,
			ScrollBarImageColor3 = COLORS.White,
			ScrollBarImageTransparency = 0.05,
			ScrollingDirection = Enum.ScrollingDirection.Y,
			ScrollingEnabled = true,
			Active = true,
			AutomaticCanvasSize = Enum.AutomaticSize.Y,
			CanvasSize = UDim2.fromScale(0, 1.1),
			ZIndex = 103,
		}, RarityFilterPanel)
		make("UIListLayout", {Padding = UDim.new(0, 7), SortOrder = Enum.SortOrder.LayoutOrder}, List)
		make("UIPadding", {
			PaddingBottom = UDim.new(0, 10),
			PaddingLeft = UDim.new(0, 25),
			PaddingTop = UDim.new(0, 10),
		}, List)

		local function updateChoiceVisual(row, check)
			local selected = normalizeRarityName(selectedEggRarity) == normalizeRarityName(row:GetAttribute("RarityName"))
			if selectedEggRarity == nil and row:GetAttribute("RarityName") == "__NONE__" then
				selected = true
			end
			row.BackgroundColor3 = selected and COLORS.Gray2 or COLORS.Black
			check.Text = selected and "✓" or ""
		end

		Clear.Activated:Connect(function()
			setSelectedEggRarity(nil)
			for _, row in ipairs(List:GetChildren()) do
				if row:IsA("TextButton") then
					local check = row:FindFirstChild("Check")
					if check then
						updateChoiceVisual(row, check)
					end
				end
			end
		end)

		local rarities = getAvailableEggRarities()
		for index, rarityInfo in ipairs(rarities) do
			local Row = make("TextButton", {
				Name = "Rarity_" .. index,
				Size = UDim2.new(1, -18, 0, 48),
				BackgroundColor3 = COLORS.Black,
				BorderSizePixel = 0,
				AutoButtonColor = false,
				Text = "",
				LayoutOrder = index,
				ZIndex = 104,
			}, List)
			Row:SetAttribute("RarityName", rarityInfo.Name)
			make("UIStroke", {Thickness = 1.15, Color = COLORS.Gray3, Transparency = 0.16}, Row)
			make("TextLabel", {
				Position = UDim2.fromScale(0.045, 0),
				Size = UDim2.fromScale(0.76, 1),
				BackgroundTransparency = 1,
				Text = rarityInfo.Name,
				TextColor3 = COLORS.White,
				Font = Enum.Font.GothamSemibold,
				TextSize = 12,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextYAlignment = Enum.TextYAlignment.Center,
				ZIndex = 105,
			}, Row)
			local Check = make("TextLabel", {
				Name = "Check",
				AnchorPoint = Vector2.new(1, 0.5),
				Position = UDim2.fromScale(0.95, 0.5),
				Size = UDim2.fromScale(0.12, 0.70),
				BackgroundTransparency = 1,
				Text = "",
				TextColor3 = COLORS.White,
				Font = Enum.Font.GothamBold,
				TextSize = 14,
				TextXAlignment = Enum.TextXAlignment.Center,
				TextYAlignment = Enum.TextYAlignment.Center,
				ZIndex = 106,
			}, Row)
			updateChoiceVisual(Row, Check)
			Row.MouseEnter:Connect(function() tween(Row, 0.10, {BackgroundColor3 = COLORS.Gray}) end)
			Row.MouseLeave:Connect(function() updateChoiceVisual(Row, Check) end)
			Row.Activated:Connect(function()
				setSelectedEggRarity(rarityInfo.Name)
				for _, otherRow in ipairs(List:GetChildren()) do
					if otherRow:IsA("TextButton") then
						local otherCheck = otherRow:FindFirstChild("Check")
						if otherCheck then
							updateChoiceVisual(otherRow, otherCheck)
						end
					end
				end
			end)
		end

		installEnglishGuard(RarityFilterOverlay)
		registerCleanup(function()
			if RarityFilterOverlay and RarityFilterOverlay.Parent then
				RarityFilterOverlay:Destroy()
			end
		end)
	end

	visualPanelOpen = true
	updateResultsScrollMode()
	updateTabListScrollMode()
	RarityFilterModal.Visible = true
	RarityFilterOverlay.Visible = true
end

local datasets = {
	Main = {
		{
			Type = "Category",
			Title = "Plot",
			Icon = "rbxassetid://72377406954512",
			Expanded = false,
			Items = {
				{
					Title = "Auto Open Eggs",
					Description = "Automatically opens eggs in your plot when they are ready.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "OpenEggs",
				},
				{
					Title = "Auto Equip Eggs",
					Description = "Automatically equips an available egg whenever no egg is currently in your hand.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "EquipEgg",
				},
				{
					Title = "Equip Best Pets",
					Description = "Automatically equips your best available pets every 5 seconds.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "EquipBestPets",
				},
				{
					Title = "Auto Place Eggs",
					Description = "Automatically places available eggs while you are inside your own plot.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "PlaceEggs",
				},
			},
		},
		{
			Type = "Category",
			Title = "Collect",
			Icon = "rbxassetid://84487893427781",
			Expanded = false,
			Items = {
				{
					Title = "Auto Redeem Codex",
					Description = "Automatically redeems all available Codex rewards every second.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "AutoRedeemCodex",
				},
				{
					Title = "Auto Collect Offline Earnings",
					Description = "Automatically collects your available offline earnings every second.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "CollectOfflineEarnings",
				},
			},
		},
	},
	["Auto Buy"] = {
		{
			Type = "Category",
			Title = "Upgrades",
			Icon = "rbxassetid://99024336494762",
			Expanded = false,
			Items = {
				{
					Title = "Auto Buy Treadmill Upgrades",
					Description = "Automatically purchases the next Treadmill upgrade whenever you meet its requirements.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "TreadmillUpgrade",
				},
				{
					Title = "Auto Buy Plot Upgrades",
					Description = "Automatically purchases the next Plot upgrade whenever you meet its requirements.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "PlotUpgrade",
				},
			},
		},
		{
			Type = "Category",
			Title = "Trail",
			Icon = "rbxassetid://129977800846764",
			Expanded = false,
			Items = {
				{
					Title = "Auto Buy Trails",
					Description = "Automatically buys the best affordable trails you do not own, prioritizing higher speed multipliers.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "BuyTrails",
				},
				{
					Title = "Equip Best Trail",
					Description = "Automatically equips the best trail you currently own.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "EquipBestTrail",
				},
			},
		},
	},
	Farm = {},
	Config = {
		{
			Type = "Category",
			Title = "Player",
			Icon = "rbxassetid://102791662357524",
			Expanded = false,
			Items = {
				{
					Title = "Auto Rejoin",
					Description = "Are you sure you want to switch servers?",
					Icon = IMAGE_CLICK,
					Compact = true,
					Action = function()
						openConfirm("Are you sure you want to switch servers?", requestServerHop)
					end,
				},
				{
					Title = "Quick Rejoin",
					Description = "Are you sure you want to quickly rejoin this same server?",
					Icon = IMAGE_CLICK,
					Compact = true,
					Action = function()
						openConfirm("Are you sure you want to quickly rejoin the same server you are currently in?", requestServerRejoin)
					end,
				},
				{
					Title = "Copy Game ID",
					Description = "Copy the current game ID to your clipboard.",
					Icon = IMAGE_CLICK,
					Compact = true,
					Action = function()
						if copyGameId() then
							showCopiedNotification("Game ID copied!", IMAGE_GAME_ID)
						end
					end,
				},
				{
					Title = "Copy Server ID",
					Description = "Copy the current server ID to your clipboard.",
					Icon = IMAGE_CLICK,
					Compact = true,
					Action = function()
						if copyServerId() then
							showCopiedNotification("Server ID copied!", IMAGE_GAME_ID)
						end
					end,
				},
				{
					Title = "Anti AFK",
					Description = "Simulate a short movement every minute while enabled.",
					Toggle = true,
					Enabled = false,
					Compact = true,
				},
					{
						Title = "Infinity Jump",
						Description = "Allows you to jump infinitely without any restrictions.",
						Toggle = true,
						Enabled = false,
						Compact = true,
					},
					{
						Title = "Auto Bat Attack",
						Description = "Automatically attacks with the bat whenever a bat is equipped.",
						Toggle = true,
						Enabled = false,
						Compact = true,
						Controller = "AutoBatAttack",
					},
					{
						Title = "Auto Equip Bat",
						Description = "Automatically equips a bat whenever one is available in your inventory.",
						Toggle = true,
						Enabled = false,
						Compact = true,
						Controller = "EquipBat",
					},
			},
		},
		{
			Type = "Category",
			Title = "Visual",
			Icon = "rbxassetid://74492590812457",
			Expanded = false,
			Items = {
				{
					Title = "Guardian ESP",
					Description = "Shows the names of the guards above their heads.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "GuardianESP",
				},
				{
					Title = "Egg ESP Settings",
					Description = "Choose which egg information should be displayed.",
					Selector = true,
					Icon = IMAGE_CLICK,
					Action = openVisualEggInfoPanel,
					Compact = true,
				},
				{
					Title = "Egg ESP",
					Description = "Enables the selected egg information on screen.",
					Toggle = true,
					Enabled = false,
					Compact = true,
					Controller = "EggESP",
				},
				{
					Title = "Egg Rarity Filter",
					Description = "Select one rarity to show Egg ESP only above matching eggs.",
					Selector = true,
					Icon = IMAGE_CLICK,
					Action = openRarityFilterPanel,
					Compact = true,
				},
			},
		},
	},

	Credits = {
		{
			Title = "By: Cleiton10HDx",
			Description = "Owner of Phantom Hub.",
			Compact = true,
		},
		{
			Title = "Discord",
			Description = "Copy the official Phantom Hub Discord server link.",
			Icon = IMAGE_CLICK,
			Compact = true,
			Action = function()
				copyDiscordLink()
				showCopiedNotification()
			end,
		},
	},
}

local function clearResults()
	for _, child in ipairs(ResultsScroll:GetChildren()) do
		if not child:IsA("UIListLayout") and not child:IsA("UIPadding") then
			child:Destroy()
		end
	end
end

local function setResultsScrollPositionY(y)
	setResultsScrollY(y)
end

local function getVisibleItems(tabName, query)
	local normalized = string.lower(query or "")

	if normalized == "" then
		return datasets[tabName] or {}
	end

	-- Search globally across every tab, but only return real options.
	-- Category headers are never search results.
	local filtered = {}
	local searchOrder = {"Main", "Auto Buy", "Farm", "Config", "Credits"}
	for _, datasetName in ipairs(searchOrder) do
		for _, item in ipairs(datasets[datasetName] or {}) do
			if item.Type == "Category" then
				for _, child in ipairs(item.Items or {}) do
					local title = string.lower(child.Title or "")
					local description = string.lower(child.Description or "")
					if string.find(title, normalized, 1, true) or string.find(description, normalized, 1, true) then
						table.insert(filtered, child)
					end
				end
			else
				local title = string.lower(item.Title or "")
				local description = string.lower(item.Description or "")
				if string.find(title, normalized, 1, true) or string.find(description, normalized, 1, true) then
					table.insert(filtered, item)
				end
			end
		end
	end

	return filtered
end

local function showEmptyState(text)
	local Empty = make("Frame", {
		Name = "EmptyState",
		Size = UDim2.fromScale(1, 0.22),
		BackgroundColor3 = COLORS.Black2,
		BorderSizePixel = 0,
		ZIndex = 4,
	}, ResultsScroll)
	make("UIStroke", {
		Thickness = 1,
		Color = COLORS.Gray2,
		Transparency = 0.28,
	}, Empty)
	make("TextLabel", {
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.fromScale(0.90, 0.68),
		BackgroundTransparency = 1,
		Text = text,
		TextColor3 = COLORS.Muted,
		Font = Enum.Font.Gotham,
		TextSize = 14,
		TextWrapped = true,
		TextXAlignment = Enum.TextXAlignment.Center,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 5,
	}, Empty)
end

local createStandardRow
local renderResults

local function createCategoryRow(category, layoutOrder, searchQuery)
	local expanded = category.Expanded == true
	local children = searchQuery ~= "" and (category._SearchItems or category.Items or {}) or (category.Items or {})

	local Row = make("Frame", {
		Name = "Category",
		Position = UDim2.fromScale(0.02, 0),
		Size = UDim2.fromScale(0.90, 0.15),
		BackgroundColor3 = COLORS.Black2,
		BorderSizePixel = 0,
		LayoutOrder = layoutOrder,
		ZIndex = 4,
	}, ResultsScroll)
	make("UIStroke", {
		Thickness = 1,
		Color = COLORS.Gray2,
		Transparency = 0.30,
	}, Row)

	local Hit = make("TextButton", {
		Name = "CategoryButton",
		Position = UDim2.fromScale(0, 0),
		Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		Text = "",
		AutoButtonColor = false,
		ZIndex = 4,
	}, Row)

	local Icon = make("ImageLabel", {
		Name = "Icon",
		Position = UDim2.fromScale(0.035, 0.20),
		Size = UDim2.fromScale(0.10, 0.60),
		BackgroundTransparency = 1,
		Image = category.Icon or "",
		ImageColor3 = COLORS.SoftWhite,
		ScaleType = Enum.ScaleType.Fit,
		ZIndex = 5,
	}, Row)

	local Title = make("TextLabel", {
		Position = UDim2.fromScale(0.16, 0),
		Size = UDim2.fromScale(0.68, 1),
		BackgroundTransparency = 1,
		Text = category.Title or "Category",
		TextColor3 = COLORS.White,
		Font = Enum.Font.GothamSemibold,
		TextSize = 14,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 5,
	}, Row)

	local Arrow = make("TextLabel", {
		AnchorPoint = Vector2.new(1, 0.5),
		Position = UDim2.fromScale(0.96, 0.5),
		Size = UDim2.fromScale(0.09, 0.50),
		BackgroundTransparency = 1,
		Text = expanded and "▼" or "▶",
		TextColor3 = COLORS.SoftWhite,
		Font = Enum.Font.GothamBold,
		TextSize = 15,
		TextXAlignment = Enum.TextXAlignment.Center,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 5,
	}, Row)

	Hit.MouseEnter:Connect(function()
		tween(Row, 0.10, {BackgroundColor3 = COLORS.DarkGray})
	end)
	Hit.MouseLeave:Connect(function()
		tween(Row, 0.10, {BackgroundColor3 = COLORS.Black2})
	end)

	Hit.Activated:Connect(function()
		-- While the Visual settings panel is open, category expand/collapse
		-- controls are temporarily disabled. Existing option states remain untouched.
		if visualPanelOpen then
			return
		end
		if searchQuery ~= "" then
			category.Expanded = true
		else
			category.Expanded = not category.Expanded
		end
		renderResults()
	end)

	if expanded or searchQuery ~= "" then
		for childIndex, child in ipairs(children) do
			createStandardRow(child, layoutOrder + childIndex)
		end
	end

	return Row
end

local toggleStates = {}
local toggleConnections = {}

local function disconnectToggle(item)
	local connection = toggleConnections[item]
	if connection then
		pcall(function()
			connection:Disconnect()
		end)
		toggleConnections[item] = nil
	end
end

local function safeRequire(moduleScript)
	if moduleScript == nil then
		return nil
	end
	local ok, result = pcall(require, moduleScript)
	if ok then
		return result
	end
	return nil
end

local function findEggTools(eggToolDisplay)
	local tools = {}
	local seen = {}

	local function scan(container)
		if not container then
			return
		end
		for _, child in ipairs(container:GetChildren()) do
			if child:IsA("Tool") and not seen[child] then
				local ok, isEgg = pcall(function()
					return eggToolDisplay and eggToolDisplay.IsEggTool and eggToolDisplay.IsEggTool(child)
				end)
				if ok and isEgg then
					seen[child] = true
					table.insert(tools, child)
				end
			end
		end
	end

	scan(LocalPlayer.Character)
	scan(LocalPlayer:FindFirstChildOfClass("Backpack"))
	return tools
end

local function getOwnPlotPlacement()
	local shared = game:GetService("ReplicatedStorage")
	local plotState = safeRequire(shared.Client:FindFirstChild("PlotState"))
	local eggRaycastModule = safeRequire(shared.Shared.Eggs:FindFirstChild("EggPlacementRaycast"))
	if not plotState or not eggRaycastModule or not eggRaycastModule.Raycast then
		return nil
	end

	local plot = plotState.ResolvePlot and plotState.ResolvePlot() or nil
	if not plot or not plot.PetArea or not plot.CenterPoint then
		return nil
	end

	local ok, raycastResult = pcall(function()
		return eggRaycastModule.Raycast(plot.PetArea, LocalPlayer.UserId)
	end)
	if not ok or raycastResult == nil or raycastResult.Instance ~= plot.PetArea then
		return nil
	end

	local relativeCFrame = plot.CenterPoint.CFrame:ToObjectSpace(CFrame.new(raycastResult.Position))
	return relativeCFrame
end

local function placeOneEgg()
	local shared = game:GetService("ReplicatedStorage")
	local eggToolDisplay = safeRequire(shared.Shared.Eggs:FindFirstChild("EggToolDisplay"))
	local eggState = safeRequire(shared.Client:FindFirstChild("EggState"))
	if not eggToolDisplay or not eggState or not eggState.PlantEgg then
		return false
	end

	local relativeCFrame = getOwnPlotPlacement()
	if not relativeCFrame then
		return false
	end

	local tools = findEggTools(eggToolDisplay)
	for _, tool in ipairs(tools) do
		local okUid, uid = pcall(function()
			return eggToolDisplay.GetToolUid and eggToolDisplay.GetToolUid(tool)
		end)
		if okUid and uid ~= nil then
			local okPlace, placed = pcall(function()
				return eggState.PlantEgg(uid, relativeCFrame)
			end)
			if okPlace and placed == true then
				local renderer = safeRequire(shared.Shared.Eggs:FindFirstChild("PlacedEggRenderer"))
				if renderer and renderer.Refresh then
					pcall(renderer.Refresh)
				end
				return true
			end
			-- A failed PlantEgg call means the current plot/egg state is not valid anymore.
			return false
		end
	end

	return false
end

local function setPlaceEggs(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			-- Auto Place Eggs now runs the placement routine directly while enabled.
			-- The previous separate inventory/hand availability gate and base-movement
			-- dependency have been removed.
			local placed = placeOneEgg()
			if placed then
				task.wait(0.35)
			else
				task.wait(0.50)
			end
		end
	end)
end

local function setEquipBestPets(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local shared = game:GetService("ReplicatedStorage")
	local remotes = safeRequire(shared.Shared:FindFirstChild("Remotes"))
	if not remotes or not remotes.Haul or not remotes.Haul.WearBest or not remotes.Haul.WearBest.InvokeServer then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			pcall(function()
				remotes.Haul.WearBest:InvokeServer()
			end)
			task.wait(5)
		end
	end)
end

local function getHumanoid()
	local character = LocalPlayer.Character
	return character and character:FindFirstChildOfClass("Humanoid") or nil
end

local function setAntiAFK(item, enabled)
	if toggleConnections[item] then
		disconnectToggle(item)
	end

	if not enabled then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			task.wait(60)
			if not running or cleanedUp then
				break
			end

			local humanoid = getHumanoid()
			if humanoid and humanoid.Health > 0 then
				humanoid:Move(Vector3.new(1, 0, 0), true)
				task.wait(0.18)
				if running and not cleanedUp and humanoid.Parent and humanoid.Health > 0 then
					humanoid:Move(Vector3.zero, true)
				end
			end
		end
	end)
end

local function setInfinityJump(item, enabled)
	disconnectToggle(item)

	if not enabled then
		return
	end

	local active = true
	local jumpConnection = UserInputService.JumpRequest:Connect(function()
		if not active or cleanedUp then
			return
		end

		local humanoid = getHumanoid()
		if humanoid and humanoid.Health > 0 then
			humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
			humanoid.Jump = true
		end
	end)

	toggleConnections[item] = {
		Disconnect = function()
			active = false
			jumpConnection:Disconnect()
		end,
	}
end

local function getEggStateModule()
	local shared = game:GetService("ReplicatedStorage")
	return safeRequire(shared.Client:FindFirstChild("EggState"))
end

local function getSaveModule()
	local shared = game:GetService("ReplicatedStorage")
	return safeRequire(shared.Shared:FindFirstChild("Save"))
end

local function getEquippedEggUID()
	local character = LocalPlayer.Character
	if not character then
		return nil
	end
	for _, child in ipairs(character:GetChildren()) do
		if child:IsA("Tool") and child:GetAttribute("ItemType") == "AssetEgg" then
			local uid = child:GetAttribute("UID")
			if typeof(uid) == "string" and uid ~= "" then
				return uid
			end
		end
	end
	return nil
end

local function eggUIDExistsInCharacterOrBackpack(uid)
	if typeof(uid) ~= "string" or uid == "" then
		return false
	end

	local function scan(container)
		if not container then
			return false
		end
		for _, child in ipairs(container:GetChildren()) do
			if child:IsA("Tool") and child:GetAttribute("ItemType") == "AssetEgg" and child:GetAttribute("UID") == uid then
				return true
			end
		end
		return false
	end

	return scan(LocalPlayer.Character) or scan(LocalPlayer:FindFirstChildOfClass("Backpack"))
end

local function getAvailableEggUID()
	local save = getSaveModule()
	if save and save.Get then
		local ok, data = pcall(save.Get)
		if ok and typeof(data) == "table" and typeof(data.EggInventory) == "table" then
			for uid, record in pairs(data.EggInventory) do
				if typeof(uid) == "string" and typeof(record) == "table" and record.Placement == nil then
					if eggUIDExistsInCharacterOrBackpack(uid) then
						return uid
					end
				end
			end
			for uid, record in pairs(data.EggInventory) do
				if typeof(uid) == "string" and typeof(record) == "table" and record.Placement == nil then
					return uid
				end
			end
		end
	end

	local eggToolDisplay = nil
	local shared = game:GetService("ReplicatedStorage")
	if shared.Shared and shared.Shared:FindFirstChild("Eggs") then
		eggToolDisplay = safeRequire(shared.Shared.Eggs:FindFirstChild("EggToolDisplay"))
	end
	for _, tool in ipairs(findEggTools(eggToolDisplay)) do
		local ok, uid = pcall(function()
			return eggToolDisplay and eggToolDisplay.GetToolUid and eggToolDisplay.GetToolUid(tool)
		end)
		if ok and typeof(uid) == "string" and uid ~= "" then
			return uid
		end
	end

	return nil
end

local function equipOneAvailableEgg(preferredUID)
	local equipped = getEquippedEggUID()
	if equipped then
		return equipped
	end

	local eggState = getEggStateModule()
	if not eggState or not eggState.WearEggTool then
		return nil
	end

	local uid = preferredUID
	if typeof(uid) ~= "string" or uid == "" then
		uid = getAvailableEggUID()
	end
	if typeof(uid) ~= "string" or uid == "" then
		return nil
	end

	local ok, equippedOk = pcall(function()
		return eggState.WearEggTool(uid)
	end)
	if ok and equippedOk then
		return uid
	end
	return nil
end

local function setEquipEgg(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local running = true
	local currentUID = nil
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			local equipped = getEquippedEggUID()
			if equipped then
				currentUID = equipped
			else
				currentUID = equipOneAvailableEgg(currentUID) or equipOneAvailableEgg(nil)
			end

			if currentUID and not eggUIDExistsInCharacterOrBackpack(currentUID) then
				currentUID = nil
			end

			task.wait(0.75)
		end
	end)
end

local function getOpenEggDependencies()
	local shared = game:GetService("ReplicatedStorage")
	local eggState = safeRequire(shared.Client:FindFirstChild("EggState"))
	local eggRecords = safeRequire(shared.Shared.Util:FindFirstChild("EggRecords"))
	local renderer = nil
	if shared.Shared:FindFirstChild("Eggs") then
		renderer = safeRequire(shared.Shared.Eggs:FindFirstChild("PlacedEggRenderer"))
	end
	return eggState, eggRecords, renderer
end

local function openReadyEggs()
	local eggState, eggRecords, renderer = getOpenEggDependencies()
	if not eggState or not eggState.ReadOwnerEggs or not eggRecords
		or not eggRecords.GrowthAlpha or not eggRecords.CurrentNightCredit
		or not renderer or not renderer.ActivateLocalEgg then
		return false
	end

	local ok, ownerEggs = pcall(function()
		return eggState.ReadOwnerEggs(LocalPlayer.UserId)
	end)
	if not ok or typeof(ownerEggs) ~= "table" then
		return false
	end

	local serverTime = workspace:GetServerTimeNow()
	local opened = false

	for uid, record in pairs(ownerEggs) do
		if typeof(uid) == "string" and typeof(record) == "table" and record.Placement then
			local ready = pcall(function()
				return eggRecords.GrowthAlpha(
					record,
					serverTime,
					record.GrowthSpeedMultiplier,
					eggRecords.CurrentNightCredit(record, serverTime, record.GrowthSpeedMultiplier),
					LocalPlayer
				) >= 1
			end)

			if ready then
				local activated, success = pcall(function()
					return renderer.ActivateLocalEgg(uid)
				end)
				if activated and success == true then
					opened = true
					task.wait(0.15)
				end
			end
		end
	end

	return opened
end

local function setOpenEggs(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			openReadyEggs()
			task.wait(1)
		end
	end)
end

local function setAutoRedeemCodex(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local shared = game:GetService("ReplicatedStorage")
	local packages = shared:FindFirstChild("Packages")
	local networking = packages and packages:FindFirstChild("Networking")
	local redeemRemote = networking and networking:FindFirstChild("RF/Codex/AskRedeemAll")
	if not redeemRemote or not redeemRemote:IsA("RemoteFunction") then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			pcall(function()
				redeemRemote:InvokeServer()
			end)
			task.wait(1)
		end
	end)
end

local function getTreadmillUpgradeClient()
	local playerScripts = LocalPlayer:FindFirstChild("PlayerScripts")
	local guiFolder = playerScripts and playerScripts:FindFirstChild("GUI")
	local treadmillScript = guiFolder and guiFolder:FindFirstChild("TreadmillUpgrade")
	local clientModule = treadmillScript and treadmillScript:FindFirstChild("Client")
	return clientModule and safeRequire(clientModule) or nil
end

local function setTreadmillUpgrade(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			local client = getTreadmillUpgradeClient()
			if client and client.CanAffordNext and client.RequestCashUpgrade then
				local shared = game:GetService("ReplicatedStorage")
				local save = safeRequire(shared.Shared:FindFirstChild("Save"))
				local affordable = false
				if save and save.Get then
					local ok, data = pcall(save.Get)
					if ok and typeof(data) == "table" then
						local okCan, result = pcall(client.CanAffordNext, data)
						affordable = okCan and result == true
					end
				end
				if affordable then
					pcall(client.RequestCashUpgrade)
				end
			end
			task.wait(1)
		end
	end)
end

local function setPlotUpgrade(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local shared = game:GetService("ReplicatedStorage")
	local baseUpgrade = safeRequire(shared.Client:FindFirstChild("BaseUpgrade"))
	if not baseUpgrade or not baseUpgrade.PurchaseNextTier or not baseUpgrade.IsNextTierAffordable then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			local save = safeRequire(shared.Shared:FindFirstChild("Save"))
			local affordable = false
			if save and save.Get then
				local ok, data = pcall(save.Get)
				if ok and typeof(data) == "table" then
					local okCan, result = pcall(baseUpgrade.IsNextTierAffordable, data)
					affordable = okCan and result == true
				end
			end
			if affordable then
				pcall(baseUpgrade.PurchaseNextTier)
			end
			task.wait(1)
		end
	end)
end

local function getTrailModules()
	local shared = game:GetService("ReplicatedStorage")
	local remotes = safeRequire(shared.Shared:FindFirstChild("Remotes"))
	local trails = safeRequire(shared.Data:FindFirstChild("Trails"))
	local save = safeRequire(shared.Shared:FindFirstChild("Save"))
	return remotes, trails, save
end

local function getTrailRank(config)
	local speed = config and config.SpeedMultiplier
	local price = config and config.Price
	if typeof(speed) ~= "number" then speed = -math.huge end
	if typeof(price) ~= "number" then price = -math.huge end
	return speed, price
end

local function setCollectOfflineEarnings(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local shared = game:GetService("ReplicatedStorage")
	local packages = shared:FindFirstChild("Packages")
	local networking = packages and packages:FindFirstChild("Networking")
	local collectRemote = networking and networking:FindFirstChild("RF/AwayEarnings/AskCollect")
	if not collectRemote or not collectRemote:IsA("RemoteFunction") then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			pcall(function()
				collectRemote:InvokeServer({Kind = "Claim"})
			end)
			task.wait(1)
		end
	end)
end

local function setBuyTrails(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local remotes, trails, save = getTrailModules()
	if not remotes or not remotes.Trailwear or not remotes.Trailwear.AskPurchase or not trails or not trails.Directory or not save or not save.Get then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			local okData, data = pcall(save.Get)
			if okData and typeof(data) == "table" then
				local money = typeof(data.Money) == "number" and data.Money or 0
				local owned = typeof(data.TrailInventory) == "table" and data.TrailInventory or {}
				local candidates = {}

				for id, config in pairs(trails.Directory) do
					if typeof(id) == "string" and typeof(config) == "table" and config.DisplayInShop and owned[id] ~= true then
						table.insert(candidates, {Id = id, Config = config})
					end
				end

				table.sort(candidates, function(a, b)
					local aSpeed, aPrice = getTrailRank(a.Config)
					local bSpeed, bPrice = getTrailRank(b.Config)
					if aSpeed ~= bSpeed then return aSpeed > bSpeed end
					if aPrice ~= bPrice then return aPrice > bPrice end
					return a.Id < b.Id
				end)

				-- Purchase only the single highest-priority affordable trail this cycle.
				-- The loop then refreshes the inventory/money and continues with the next best trail.
				for _, entry in ipairs(candidates) do
					local price = entry.Config.Price
					if typeof(price) == "number" and price >= 0 and money >= price then
						pcall(function()
							remotes.Trailwear.AskPurchase:InvokeServer(entry.Id)
						end)
						break
					end
				end
			end

			task.wait(1)
		end
	end)
end

local function setEquipBestTrail(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local remotes, trails, save = getTrailModules()
	if not remotes or not remotes.Trailwear or not remotes.Trailwear.AskChoose or not trails or not trails.Directory or not save or not save.Get then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			local okData, data = pcall(save.Get)
			if okData and typeof(data) == "table" and typeof(data.TrailInventory) == "table" then
				local bestId, bestConfig = nil, nil
				for id, owned in pairs(data.TrailInventory) do
					if owned == true and trails.Directory[id] then
						local config = trails.Directory[id]
						if not bestConfig then
							bestId, bestConfig = id, config
						else
							local speed, price = getTrailRank(config)
							local bestSpeed, bestPrice = getTrailRank(bestConfig)
							if speed > bestSpeed or (speed == bestSpeed and (price > bestPrice or (price == bestPrice and id < bestId))) then
								bestId, bestConfig = id, config
							end
						end
					end
				end
				if bestId and data.EquippedTrail ~= bestId then
					pcall(function()
						remotes.Trailwear.AskChoose:InvokeServer(bestId)
					end)
				end
			end
			task.wait(1)
		end
	end)
end

local function isBatTool(tool)
	if not tool or not tool:IsA("Tool") then
		return false
	end

	local name = string.lower(tool.Name or "")
	return string.find(name, "bat", 1, true) ~= nil
		or string.find(name, "bastão", 1, true) ~= nil
		or string.find(name, "bastao", 1, true) ~= nil
		or string.find(name, "volcano", 1, true) ~= nil
end

local function findBatTool()
	local character = LocalPlayer.Character
	if character then
		for _, child in ipairs(character:GetChildren()) do
			if isBatTool(child) then
				return child
			end
		end
	end

	local backpack = LocalPlayer:FindFirstChildOfClass("Backpack")
	if backpack then
		for _, child in ipairs(backpack:GetChildren()) do
			if isBatTool(child) then
				return child
			end
		end
	end

	return nil
end

local function getEquippedBat()
	local character = LocalPlayer.Character
	if not character then
		return nil
	end

	for _, child in ipairs(character:GetChildren()) do
		if isBatTool(child) then
			return child
		end
	end

	return nil
end

local function setEquipBat(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			local character = LocalPlayer.Character
			local humanoid = character and character:FindFirstChildOfClass("Humanoid")
			if humanoid and humanoid.Health > 0 and not getEquippedBat() then
				local bat = findBatTool()
				if bat and bat.Parent ~= character then
					pcall(function()
						humanoid:EquipTool(bat)
					end)
				end
			end
			task.wait(0.35)
		end
	end)
end

local function setAutoBatAttack(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local running = true
	toggleConnections[item] = {
		Disconnect = function()
			running = false
		end,
	}

	task.spawn(function()
		while running and not cleanedUp do
			local tool = getEquippedBat()
			if tool then
				pcall(function()
					tool:Activate()
				end)
			end
			task.wait(0.35)
		end
	end)
end

local function getGuardianModelFromArea(area)
	if not area or not area:IsA("Model") then
		return nil
	end

	-- The supplied GuardAreas client script resolves each area's actual guard
	-- through GuardComponent:GetGuardModel(). We mirror that result as closely
	-- as possible without instantiating a second GuardComponent.
	local directGuard = area:FindFirstChild("Guard")
	if directGuard and directGuard:IsA("Model") then
		local humanoid = directGuard:FindFirstChildOfClass("Humanoid")
		local root = directGuard:FindFirstChild("HumanoidRootPart") or directGuard:FindFirstChild("Head")
		if humanoid and root then
			return directGuard
		end
	end

	for _, child in ipairs(area:GetChildren()) do
		if child:IsA("Model") then
			local humanoid = child:FindFirstChildOfClass("Humanoid")
			local root = child:FindFirstChild("HumanoidRootPart") or child:FindFirstChild("Head")
			if humanoid and root then
				return child
			end
		end
	end

	for _, descendant in ipairs(area:GetDescendants()) do
		if descendant:IsA("Model") then
			local humanoid = descendant:FindFirstChildOfClass("Humanoid")
			local root = descendant:FindFirstChild("HumanoidRootPart") or descendant:FindFirstChild("Head")
			if humanoid and root then
				return descendant
			end
		end
	end

	return nil
end

local function getGuardianDisplayName(guardModel, area)
	if not area then
		return "Guardian"
	end

	-- Use the guard-area model name supplied by the game's GuardAreas system
	-- instead of the humanoid/model name. Example: Forest -> "Guardian of the Forest".
	local areaName = area.Name
	if type(areaName) == "string" and areaName ~= "" then
		return "Guardian of the " .. areaName
	end

	return "Guardian"
end

local function setGuardianESP(item, enabled)
	disconnectToggle(item)
	if not enabled then
		return
	end

	local running = true
	local labels = {}

	local function destroyLabel(model)
		local label = labels[model]
		if label then
			labels[model] = nil
			pcall(function()
				label:Destroy()
			end)
		end
	end

	local function destroyAllLabels()
		for model, label in pairs(labels) do
			labels[model] = nil
			pcall(function()
				label:Destroy()
			end)
		end
	end

	local function createOrUpdateLabel(guardModel, area)
		if not guardModel or not guardModel.Parent then
			return
		end

		local adornee = guardModel:FindFirstChild("Head") or guardModel:FindFirstChild("HumanoidRootPart")
		if not adornee or not adornee:IsA("BasePart") then
			return
		end

		local label = labels[guardModel]
		if label and label.Parent then
			local textLabel = label:FindFirstChild("Name")
			if textLabel and textLabel:IsA("TextLabel") then
				textLabel.Text = getGuardianDisplayName(guardModel, area)
				label.Adornee = adornee
			end
			return
		end

		label = Instance.new("BillboardGui")
		label.Name = "PhantomHub_GuardianESP"
		label.Adornee = adornee
		label.AlwaysOnTop = true
		label.LightInfluence = 0
		label.MaxDistance = 1000
		label.Size = UDim2.fromOffset(110, 20)
		label.StudsOffsetWorldSpace = Vector3.new(0, 2.55, 0)
		label.ResetOnSpawn = false

		local textLabel = Instance.new("TextLabel")
		textLabel.Name = "Name"
		textLabel.Size = UDim2.fromScale(1, 1)
		textLabel.BackgroundTransparency = 1
		textLabel.BorderSizePixel = 0
		textLabel.Text = getGuardianDisplayName(guardModel, area)
		textLabel.TextColor3 = Color3.fromRGB(255, 45, 45)
		textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
		textLabel.TextStrokeTransparency = 0.25
		textLabel.Font = Enum.Font.GothamBold
		textLabel.TextSize = 11
		textLabel.TextScaled = false
		textLabel.TextWrapped = false
		textLabel.TextXAlignment = Enum.TextXAlignment.Center
		textLabel.TextYAlignment = Enum.TextYAlignment.Center
		textLabel.Parent = label

		label.Parent = PlayerGui
		labels[guardModel] = label
	end

	local function refresh()
		local seen = {}
		local guardAreasFolder = workspace:FindFirstChild("__OBJECTS")
		guardAreasFolder = guardAreasFolder and guardAreasFolder:FindFirstChild("Areas")
		guardAreasFolder = guardAreasFolder and guardAreasFolder:FindFirstChild("GuardAreas")
		if not guardAreasFolder then
			destroyAllLabels()
			return
		end

		for _, area in ipairs(guardAreasFolder:GetChildren()) do
			if area:IsA("Model") then
				local guardModel = getGuardianModelFromArea(area)
				if guardModel then
					seen[guardModel] = true
					createOrUpdateLabel(guardModel, area)
				end
			end
		end

		for model in pairs(labels) do
			if not seen[model] or not model.Parent then
				destroyLabel(model)
			end
		end
	end

	toggleConnections[item] = {
		Disconnect = function()
			running = false
			destroyAllLabels()
		end,
	}

	task.spawn(function()
		refresh()
		while running and not cleanedUp do
			task.wait(0.40)
			if running and not cleanedUp then
				refresh()
			end
		end
		destroyAllLabels()
	end)
end


local function getEggDisplayName(record, model)
	-- Prefer the game's shared asset configuration for the actual egg name.
	local shared = game:GetService("ReplicatedStorage")
	local assetsModule = shared:FindFirstChild("Data") and shared.Data:FindFirstChild("Assets")
	local assets = safeRequire(assetsModule)
	local asset = nil

	if assets and type(assets) == "table" and type(assets.Directory) == "table" and record then
		asset = assets.Directory[record.AssetCategory]
	end

	if type(asset) == "table" then
		local candidates = {
			asset.DisplayName,
			asset.Name,
			asset.Title,
			asset.Display,
			asset.AssetName,
		}
		for _, value in ipairs(candidates) do
			if type(value) == "string" and value ~= "" then
				return value
			end
		end
	end

	if model then
		local candidates = {
			model:GetAttribute("DisplayName"),
			model:GetAttribute("Name"),
			model:GetAttribute("EggName"),
		}
		for _, value in ipairs(candidates) do
			if type(value) == "string" and value ~= "" then
				return value
			end
		end
	end

	if record and type(record.AssetCategory) == "string" and record.AssetCategory ~= "" then
		return record.AssetCategory
	end

	return "Egg"
end

local function getEggESPStateModule()
	local shared = game:GetService("ReplicatedStorage")
	return safeRequire(shared:FindFirstChild("Client") and shared.Client:FindFirstChild("EggState"))
end

local function getEggRarityInfo(record)
	local shared = game:GetService("ReplicatedStorage")
	local assetsModule = shared:FindFirstChild("Data") and shared.Data:FindFirstChild("Assets")
	local assets = safeRequire(assetsModule)

	if not assets or type(assets) ~= "table" or type(assets.Directory) ~= "table" or not record then
		return nil, Color3.fromRGB(255, 255, 255)
	end

	local asset = assets.Directory[record.AssetCategory]
	if type(asset) ~= "table" or type(asset.Rarity) ~= "table" then
		return nil, Color3.fromRGB(255, 255, 255)
	end

	local rarity = asset.Rarity
	local rarityName = rarity.DisplayName or rarity.Name or rarity.Title
	if type(rarityName) ~= "string" or rarityName == "" then
		rarityName = record.Rarity or record.RarityName
	end

	if type(rarityName) ~= "string" or rarityName == "" then
		rarityName = "Unknown"
	end

	local rarityColor = rarity.Color
	if typeof(rarityColor) ~= "Color3" then
		rarityColor = Color3.fromRGB(255, 255, 255)
	end

	return rarityName, rarityColor
end

local function getEggMutationName(record)
	if not record then
		return nil
	end

	local function extractMutation(value)
		if type(value) == "string" then
			if value ~= "" then
				return value
			end
			return nil
		end

		if type(value) == "table" then
			local directCandidates = {
				value.DisplayName,
				value.Name,
				value.Title,
				value.Mutation,
				value.Type,
				value.Id,
			}

			for _, candidate in ipairs(directCandidates) do
				if type(candidate) == "string" and candidate ~= "" then
					return candidate
				end
			end

			for key, candidate in pairs(value) do
				if type(candidate) == "string" and candidate ~= "" then
					if key == "Name" or key == "DisplayName" or key == "Title" or key == "Mutation" or key == "Type" or key == "Id" then
						return candidate
					end
				elseif candidate == true and type(key) == "string" and key ~= "" then
					return key
				end
			end

			for _, candidate in ipairs(value) do
				local extracted = extractMutation(candidate)
				if extracted then
					return extracted
				end
			end
		end

		return nil
	end

	local mutation = extractMutation(record.Mutations)
	if mutation then
		return mutation
	end

	return extractMutation(record.BaseMutation)
end

local function setEggESP(item, enabled)
	disconnectToggle(item)

	if not enabled then
		if refreshEggESP then
			pcall(refreshEggESP, true)
		end
		return
	end

	local running = true
	local labels = {}
	local eggState = getEggESPStateModule()

	local function destroyLabel(model)
		local label = labels[model]
		if label then
			labels[model] = nil
			pcall(function()
				label:Destroy()
			end)
		end
	end

	local function destroyAllLabels()
		for model, label in pairs(labels) do
			labels[model] = nil
			pcall(function()
				label:Destroy()
			end)
		end
	end

	local function getEggAdornee(model)
		return model.PrimaryPart
			or model:FindFirstChild("HumanoidRootPart")
			or model:FindFirstChildWhichIsA("BasePart")
	end

	local function clearInfoLabels(label)
		for _, child in ipairs(label:GetChildren()) do
			if child:IsA("TextLabel") then
				child:Destroy()
			end
		end
	end

	local function addInfoLine(label, name, text, textColor, order)
		local line = Instance.new("TextLabel")
		line.Name = name
		line.Size = UDim2.fromScale(1, 0)
		line.AutomaticSize = Enum.AutomaticSize.Y
		line.BackgroundTransparency = 1
		line.BorderSizePixel = 0
		line.Text = name .. ": " .. text
		line.TextColor3 = textColor or Color3.fromRGB(255, 255, 255)
		line.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
		line.TextStrokeTransparency = 0.30
		line.Font = Enum.Font.GothamSemibold
		line.TextSize = 10
		line.TextScaled = false
		line.TextWrapped = false
		line.TextXAlignment = Enum.TextXAlignment.Center
		line.TextYAlignment = Enum.TextYAlignment.Center
		line.LayoutOrder = order
		line.ZIndex = 2
		line.Parent = label
	end

	local function createOrUpdateLabel(model, record)
		if not model or not model.Parent then
			return
		end

		local hasAnySelection = visualSelectionState.Name
			or visualSelectionState.Distance
			or visualSelectionState.Mutation
			or visualSelectionState.Rarity

		if not hasAnySelection then
			destroyLabel(model)
			return
		end

		-- A selected rarity acts as a filter for the complete Egg ESP label.
		-- With no rarity selected, all eggs continue to behave as before.
		if selectedEggRarity ~= nil then
			local rarityName = getEggRarityInfo(record)
			if normalizeRarityName(rarityName) ~= normalizeRarityName(selectedEggRarity) then
				destroyLabel(model)
				return
			end
		end

		local adornee = getEggAdornee(model)
		if not adornee or not adornee:IsA("BasePart") then
			destroyLabel(model)
			return
		end

		local label = labels[model]
		if not label or not label.Parent then
			label = Instance.new("BillboardGui")
			label.Name = "PhantomHub_EggESP"
			label.Adornee = adornee
			label.AlwaysOnTop = true
			label.LightInfluence = 0
			label.MaxDistance = 1000
			label.Size = UDim2.fromOffset(150, 80)
			label.StudsOffsetWorldSpace = Vector3.new(0, 2.05, 0)
			label.ResetOnSpawn = false

			local layout = Instance.new("UIListLayout")
			layout.Name = "InfoLayout"
			layout.FillDirection = Enum.FillDirection.Vertical
			layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
			layout.VerticalAlignment = Enum.VerticalAlignment.Center
			layout.SortOrder = Enum.SortOrder.LayoutOrder
			layout.Padding = UDim.new(0, 0)
			layout.Parent = label

			label.Parent = PlayerGui
			labels[model] = label
		end

		label.Adornee = adornee
		clearInfoLabels(label)

		local order = 1

		if visualSelectionState.Name then
			addInfoLine(label, "Name", getEggDisplayName(record, model), Color3.fromRGB(255, 255, 255), order)
			order += 1
		end

		if visualSelectionState.Distance then
			local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
			local rootPart = root and root:IsA("BasePart") and root or nil
			if not rootPart then
				local character = LocalPlayer.Character
				if character then
					rootPart = character:FindFirstChildWhichIsA("BasePart")
				end
			end

			if rootPart then
				local distance = (rootPart.Position - adornee.Position).Magnitude
				local roundedDistance = math.floor(distance + 0.5)
				addInfoLine(label, "Distance", tostring(roundedDistance) .. " studs", Color3.fromRGB(255, 255, 255), order)
			else
				addInfoLine(label, "Distance", "-- studs", Color3.fromRGB(255, 255, 255), order)
			end
			order += 1
		end

		if visualSelectionState.Mutation then
			local mutationName = getEggMutationName(record)
			if mutationName then
				addInfoLine(label, "Mutation", mutationName, Color3.fromRGB(255, 255, 255), order)
				order += 1
			end
		end

		if visualSelectionState.Rarity then
			local rarityName, rarityColor = getEggRarityInfo(record)
			addInfoLine(label, "Rarity", rarityName or "Unknown", rarityColor, order)
			order += 1
		end
	end

	local function refresh(forceDestroy)
		if forceDestroy then
			destroyAllLabels()
			return
		end

		local hasAnySelection = visualSelectionState.Name
			or visualSelectionState.Distance
			or visualSelectionState.Mutation
			or visualSelectionState.Rarity

		if not hasAnySelection then
			destroyAllLabels()
			return
		end

		local folder = workspace:FindFirstChild("AreaEggSlotsClient")
		if not folder then
			destroyAllLabels()
			return
		end

		local seen = {}

		for _, model in ipairs(folder:GetChildren()) do
			if model:IsA("Model") then
				local record = nil

				if eggState and eggState.ReadFieldEgg then
					pcall(function()
						record = eggState.ReadFieldEgg(model.Name)
					end)
				end

				if record then
					seen[model] = true
					createOrUpdateLabel(model, record)
				end
			end
		end

		for model in pairs(labels) do
			if not seen[model] or not model.Parent then
				destroyLabel(model)
			end
		end
	end

	refreshEggESP = function(forceDestroy)
		if not running then
			return
		end
		refresh(forceDestroy)
	end

	toggleConnections[item] = {
		Disconnect = function()
			running = false
			if refreshEggESP then
				refreshEggESP = nil
			end
			destroyAllLabels()
		end,
	}

	task.spawn(function()
		refresh()
		while running and not cleanedUp do
			task.wait(0.20)
			if running and not cleanedUp then
				refresh()
			end
		end
		destroyAllLabels()
	end)
end

local function setToggleState(item, enabled)
	toggleStates[item] = enabled == true

	if item.Controller == "AutoRedeemCodex" then
		setAutoRedeemCodex(item, toggleStates[item])
	elseif item.Controller == "TreadmillUpgrade" then
		setTreadmillUpgrade(item, toggleStates[item])
	elseif item.Controller == "PlotUpgrade" then
		setPlotUpgrade(item, toggleStates[item])
	elseif item.Controller == "CollectOfflineEarnings" then
		setCollectOfflineEarnings(item, toggleStates[item])
	elseif item.Controller == "BuyTrails" then
		setBuyTrails(item, toggleStates[item])
	elseif item.Controller == "EquipBestTrail" then
		setEquipBestTrail(item, toggleStates[item])
	elseif item.Controller == "OpenEggs" then
		setOpenEggs(item, toggleStates[item])
	elseif item.Controller == "PlaceEggs" then
		setPlaceEggs(item, toggleStates[item])
	elseif item.Controller == "EquipBestPets" then
		setEquipBestPets(item, toggleStates[item])
	elseif item.Controller == "EquipEgg" then
		setEquipEgg(item, toggleStates[item])
	elseif item.Title == "Anti AFK" then
		setAntiAFK(item, toggleStates[item])
	elseif item.Title == "Infinity Jump" then
		setInfinityJump(item, toggleStates[item])
	elseif item.Controller == "AutoBatAttack" then
		setAutoBatAttack(item, toggleStates[item])
	elseif item.Controller == "EquipBat" then
		setEquipBat(item, toggleStates[item])
	elseif item.Controller == "GuardianESP" then
		setGuardianESP(item, toggleStates[item])
	elseif item.Controller == "EggESP" then
		setEggESP(item, toggleStates[item])
	end
end

createStandardRow = function(item, layoutOrder)
	local Row = make("Frame", {
		Name = "Option",
		Position = UDim2.fromScale(0.02, 0),
		Size = UDim2.fromScale(item.Compact and 0.90 or 0.90, 0.15),
		BackgroundColor3 = COLORS.Black2,
		BorderSizePixel = 0,
		LayoutOrder = layoutOrder,
		ZIndex = 4,
	}, ResultsScroll)
	make("UIStroke", {Thickness = 1, Color = COLORS.Gray2, Transparency = 0.30}, Row)

	make("TextLabel", {
		Position = UDim2.fromScale(0.035, 0.16), Size = UDim2.fromScale(0.73, 0.32),
		BackgroundTransparency = 1, Text = item.Title or "Option", TextColor3 = COLORS.White,
		Font = Enum.Font.GothamSemibold, TextSize = 14, TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center, ZIndex = 5,
	}, Row)

	make("TextLabel", {
		Position = UDim2.fromScale(0.035, 0.49), Size = UDim2.fromScale(0.73, 0.34),
		BackgroundTransparency = 1, Text = item.Description or "", TextColor3 = COLORS.Muted,
		Font = Enum.Font.Gotham, TextSize = 11, TextWrapped = true,
		TextXAlignment = Enum.TextXAlignment.Left, TextYAlignment = Enum.TextYAlignment.Top, ZIndex = 5,
	}, Row)

	if item.Selector then
		local Right = make("TextButton", {
			Name = "SelectorButton",
			AnchorPoint = Vector2.new(1, 0.5),
			Position = UDim2.fromScale(0.95, 0.5),
			Size = UDim2.fromScale(0.16, 0.46),
			BackgroundColor3 = COLORS.Gray,
			BorderSizePixel = 0,
			AutoButtonColor = false,
			Text = "Open",
			TextColor3 = COLORS.White,
			Font = Enum.Font.GothamSemibold,
			TextSize = 11,
			ZIndex = 7,
		}, Row)
		make("UICorner", {CornerRadius = UDim.new(0, 8)}, Right)
		Right.MouseEnter:Connect(function() tween(Right, 0.10, {BackgroundColor3 = COLORS.Gray3}) end)
		Right.MouseLeave:Connect(function() tween(Right, 0.10, {BackgroundColor3 = COLORS.Gray}) end)
		Right.Activated:Connect(function()
			if item.Action then item.Action() end
		end)
		return Row
	end

	if item.Toggle then
		local ToggleButton = make("TextButton", {
			Name = "Toggle", AnchorPoint = Vector2.new(1, 0.5), Position = UDim2.fromScale(0.95, 0.5),
			Size = UDim2.fromScale(0.13, 0.44), BackgroundColor3 = COLORS.Gray, BorderSizePixel = 0,
			AutoButtonColor = false, Text = "", ZIndex = 7,
		}, Row)
		make("UICorner", {CornerRadius = UDim.new(1, 0)}, ToggleButton)
		local Knob = make("Frame", {
			Name = "Knob", AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.fromScale(0.23, 0.5),
			Size = UDim2.fromScale(0.34, 0.78), BackgroundColor3 = COLORS.White, BorderSizePixel = 0, ZIndex = 8,
		}, ToggleButton)
		make("UICorner", {CornerRadius = UDim.new(1, 0)}, Knob)

		if toggleStates[item] == nil then
			toggleStates[item] = item.Enabled == true
		end

		local function updateToggle(animate)
			local on = toggleStates[item] == true
			local targetColor = on and COLORS.Gray3 or COLORS.Gray
			local targetPosition = UDim2.fromScale(on and 0.77 or 0.23, 0.5)

			if animate then
				tween(ToggleButton, 0.16, {BackgroundColor3 = targetColor})
				tween(Knob, 0.16, {Position = targetPosition})
			else
				ToggleButton.BackgroundColor3 = targetColor
				Knob.Position = targetPosition
			end
		end

		-- Initial rendering is applied instantly, so changing tabs, opening/closing
		-- the menu, or opening/closing categories never makes the switch appear OFF
		-- for a frame before returning to its saved state.
		updateToggle(false)

		ToggleButton.Activated:Connect(function()
			setToggleState(item, not (toggleStates[item] == true))
			updateToggle(true)
		end)
		return Row
	end

	local Right = make("ImageButton", {
		AnchorPoint = Vector2.new(1, 0.5), Position = UDim2.fromScale(0.95, 0.5),
		Size = UDim2.fromScale(0.09, 0.48), BackgroundTransparency = 1, AutoButtonColor = false,
		Image = item.Icon or IMAGE_CLICK, ImageColor3 = COLORS.SoftWhite, ScaleType = Enum.ScaleType.Fit,
		Visible = item.Icon ~= nil, Active = item.Icon ~= nil, ZIndex = 6,
	}, Row)
	make("UIAspectRatioConstraint", {AspectRatio = 1}, Right)

	local Hit = make("TextButton", {
		Name = "ClickSurface", Position = UDim2.fromScale(0, 0), Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1, Text = "", AutoButtonColor = false, ZIndex = 4,
	}, Row)
	Hit.MouseEnter:Connect(function() tween(Row, 0.10, {BackgroundColor3 = COLORS.DarkGray}) end)
	Hit.MouseLeave:Connect(function() tween(Row, 0.10, {BackgroundColor3 = COLORS.Black2}) end)
	if item.Action then
		Hit.Activated:Connect(item.Action)
		Right.Activated:Connect(item.Action)
	end
	return Row
end

renderResults = function()
	local previousScrollY = ResultsScroll.CanvasPosition.Y
	clearResults()
	local query = SearchBox.Text
	local items = getVisibleItems(currentTab, query)
	if #items == 0 then
		if currentTab == "Main" and query == "" then
			setResultsScrollPositionY(0)
			return
		elseif query ~= "" then
			showEmptyState("No matching options found.")
		elseif currentTab == "Auto Buy" then
			setResultsScrollPositionY(0)
			return
		elseif currentTab == "Config" then
			showEmptyState("No options are available in Config.")
		elseif currentTab == "Credits" then
			showEmptyState("No options are available in Credits.")
		else
			showEmptyState("This tab has no options yet.")
		end
		setResultsScrollPositionY(0)
		return
	end

	local layoutIndex = 0
	if query ~= "" then
		-- Global search results are option-only; categories are never rendered.
		for _, item in ipairs(items) do
			layoutIndex += 1
			createStandardRow(item, layoutIndex)
		end
		task.defer(function()
			setResultsScrollPositionY(previousScrollY)
		end)
		return
	end

	for _, item in ipairs(items) do
		layoutIndex += 1
		if item.Type == "Category" then
			createCategoryRow(item, layoutIndex, query)
			if item.Expanded then
				layoutIndex += #(item.Items or {})
			end
		else
			createStandardRow(item, layoutIndex)
		end
	end

	task.defer(function()
		setResultsScrollPositionY(previousScrollY)
		applyEnglishOnly(ResultsScroll)
	end)
	applyEnglishOnly(ResultsScroll)
	-- Garantir que o canvas size seja recalculado corretamente para melhor scrolling
	task.wait()
	local listLayout = ResultsScroll:FindFirstChildOfClass("UIListLayout")
	if listLayout then
		local canvasYSize = listLayout.AbsoluteContentSize.Y
		if canvasYSize > 0 then
			ResultsScroll.CanvasSize = UDim2.fromScale(1, math.max(1, (canvasYSize + 10) / ResultsScroll.AbsoluteSize.Y))
		end
	end
end

local function setTab(name)
	-- The Visual panel temporarily locks tab switching.
	-- Existing options/automation remain untouched.
	if visualPanelOpen then
		return
	end
	currentTab = name
	updateResultsScrollMode()
	for tabName, button in pairs(tabButtons) do
		local selected = tabName == name
		tween(button, 0.14, {
			BackgroundColor3 = selected and COLORS.Gray2 or COLORS.Black2,
			BackgroundTransparency = selected and 0 or 0.15,
		})
		local icon = button:FindFirstChild("Icon")
		if icon and icon:IsA("ImageLabel") then
			tween(icon, 0.14, {ImageColor3 = selected and COLORS.White or COLORS.SoftWhite})
		end
		local label = button:FindFirstChild("Label")
		if label and label:IsA("TextLabel") then
			tween(label, 0.14, {TextColor3 = selected and COLORS.White or COLORS.SoftWhite})
		end
	end
	SearchBox.Text = ""
	SearchHint.Visible = true
	SearchClear.Visible = false
	renderResults()
end

for index, tabInfo in ipairs(tabs) do
	local Tab = make("TextButton", {
		Name = tabInfo.Name,
		Size = UDim2.fromScale(1, 0.105),
		BackgroundColor3 = COLORS.Black2,
		BackgroundTransparency = 0.15,
		BorderSizePixel = 0,
		AutoButtonColor = false,
		Text = "",
		LayoutOrder = index,
		ZIndex = 5,
	}, TabList)
	make("UIStroke", {
		Thickness = 1,
		Color = COLORS.Gray,
		Transparency = 0.55,
	}, Tab)

	make("ImageLabel", {
		Name = "Icon",
		Position = UDim2.fromScale(0.065, 0.22),
		Size = UDim2.fromScale(0.18, 0.56),
		BackgroundTransparency = 1,
		Image = tabInfo.Icon,
		ImageColor3 = COLORS.SoftWhite,
		ScaleType = Enum.ScaleType.Fit,
		ZIndex = 6,
	}, Tab)

	make("TextLabel", {
		Name = "Label",
		Position = UDim2.fromScale(0.30, 0),
		Size = UDim2.fromScale(0.62, 1),
		BackgroundTransparency = 1,
		Text = tabInfo.Name,
		TextColor3 = COLORS.SoftWhite,
		Font = Enum.Font.GothamSemibold,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		ZIndex = 6,
	}, Tab)

	Tab.MouseEnter:Connect(function()
		if currentTab ~= tabInfo.Name then
			tween(Tab, 0.11, {BackgroundColor3 = COLORS.DarkGray, BackgroundTransparency = 0.05})
		end
	end)
	Tab.MouseLeave:Connect(function()
		if currentTab ~= tabInfo.Name then
			tween(Tab, 0.11, {BackgroundColor3 = COLORS.Black2, BackgroundTransparency = 0.15})
		end
	end)
	Tab.Activated:Connect(function()
		-- Do not allow changing tabs while the Visual settings panel is open.
		if visualPanelOpen then
			return
		end
		setTab(tabInfo.Name)
	end)

	tabButtons[tabInfo.Name] = Tab
end

local ConfirmOverlay = make("Frame", {
	Name = "ConfirmOverlay",
	Position = UDim2.fromScale(0, 0),
	Size = UDim2.fromScale(1, 1),
	BackgroundColor3 = COLORS.Black,
	BackgroundTransparency = 0.42,
	BorderSizePixel = 0,
	Visible = false,
	Active = true,
	ZIndex = 1000,
}, ScreenGui)

local ConfirmPanel = make("Frame", {
	Name = "ConfirmPanel",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.fromScale(0.44, 0.25),
	BackgroundColor3 = COLORS.Black2,
	BorderSizePixel = 0,
	Active = true,
	ZIndex = 1001,
}, ConfirmOverlay)
make("UIAspectRatioConstraint", {AspectRatio = 1.75}, ConfirmPanel)
make("UIStroke", {
	Thickness = 1.4,
	Color = COLORS.Gray3,
	Transparency = 0.08,
}, ConfirmPanel)

local ConfirmDescription = make("TextLabel", {
	Position = UDim2.fromScale(0.08, 0.10),
	Size = UDim2.fromScale(0.84, 0.45),
	BackgroundTransparency = 1,
	Text = "Are you sure you want to remove Phantom Hub and stop all of its active features?",
	TextColor3 = COLORS.White,
	Font = Enum.Font.Gotham,
	TextSize = 12,
	TextWrapped = true,
	TextXAlignment = Enum.TextXAlignment.Center,
	TextYAlignment = Enum.TextYAlignment.Center,
	ZIndex = 1002,
}, ConfirmPanel)

local ConfirmOK = make("TextButton", {
	Position = UDim2.fromScale(0.08, 0.66),
	Size = UDim2.fromScale(0.39, 0.20),
	BackgroundColor3 = COLORS.Gray,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Active = true,
	Selectable = false,
	Text = "Confirm",
	TextColor3 = Color3.fromRGB(75, 190, 105),
	Font = Enum.Font.GothamBold,
	TextSize = 12,
	ZIndex = 1003,
}, ConfirmPanel)

local ConfirmCancel = make("TextButton", {
	Position = UDim2.fromScale(0.53, 0.66),
	Size = UDim2.fromScale(0.39, 0.20),
	BackgroundColor3 = COLORS.Gray,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Active = true,
	Selectable = false,
	Text = "Cancel",
	TextColor3 = Color3.fromRGB(220, 75, 75),
	Font = Enum.Font.GothamBold,
	TextSize = 12,
	ZIndex = 1003,
}, ConfirmPanel)

openConfirm = function(description, action)
	if cleanedUp or not ScreenGui.Parent then
		return
	end

	ConfirmDescription.Text = description
	pendingAction = action
	ConfirmOverlay.Visible = true
	ConfirmOverlay.ZIndex = 1000
	ConfirmPanel.Position = UDim2.fromScale(0.5, 0.5)
	ConfirmPanel.Size = UDim2.fromScale(0.42, 0.24)
	tween(ConfirmPanel, 0.18, {
		Size = UDim2.fromScale(0.44, 0.25),
		Position = UDim2.fromScale(0.5, 0.5),
	})
end

local function closeConfirm()
	pendingAction = nil
	ConfirmOverlay.Visible = false
	ConfirmPanel.Position = UDim2.fromScale(0.5, 0.5)
end

local confirmBusy = false

local function activateConfirmCancel()
	if confirmBusy then
		return
	end
	confirmBusy = true
	closeConfirm()
	task.defer(function()
		confirmBusy = false
	end)
end

local function activateConfirmOK()
	if confirmBusy then
		return
	end
	confirmBusy = true
	local action = pendingAction
	closeConfirm()
	if action then
		local ok, err = pcall(action)
		if not ok then
		warn("Phantom Hub cleanup error:", err)
		end
	end
	confirmBusy = false
end

ConfirmCancel.Activated:Connect(activateConfirmCancel)
ConfirmOK.Activated:Connect(activateConfirmOK)

ConfirmCancel.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		activateConfirmCancel()
	end
end)

ConfirmOK.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		activateConfirmOK()
	end
end)

local BUTTON_COLORS = {
	Neutral = COLORS.Gray,
	NeutralHover = COLORS.Gray2,
	ConfirmText = Color3.fromRGB(75, 190, 105),
	CancelText = Color3.fromRGB(220, 75, 75),
}

for _, button in ipairs({ConfirmCancel, ConfirmOK}) do
	button.MouseEnter:Connect(function()
		tween(button, 0.1, {BackgroundColor3 = BUTTON_COLORS.NeutralHover})
	end)
	button.MouseLeave:Connect(function()
		tween(button, 0.1, {BackgroundColor3 = BUTTON_COLORS.Neutral})
	end)
end

local function hideMenu()
	if not Menu.Parent then
		return
	end
	Menu.Visible = false
	ConfirmOverlay.Visible = false
	LockOverlay.Visible = false
	closeVisualEggInfoPanel()
end

local function showMenu()
	if not Menu.Parent or cleanedUp then
		return
	end
	Menu.Visible = true
	setTab(currentTab)
end

MinimizeButton.Activated:Connect(function()
	hideMenu()
end)

local closeBusy = false

local function requestCloseFromButton()
	if closeBusy or cleanedUp or not ScreenGui.Parent then
		return
	end
	closeBusy = true
	openConfirm(
		"Are you sure you want to remove Phantom Hub and stop all of its active features?",
		function()
			cleanupAll()
			if ScreenGui.Parent then
				ScreenGui:Destroy()
			end
		end
	)
	task.defer(function()
		closeBusy = false
	end)
end

CloseButton.Activated:Connect(requestCloseFromButton)
CloseButton.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		requestCloseFromButton()
	end
end)

local function isPointInside(guiObject, point)
	local position = guiObject.AbsolutePosition
	local size = guiObject.AbsoluteSize
	return point.X >= position.X and point.X <= position.X + size.X
		and point.Y >= position.Y and point.Y <= position.Y + size.Y
end

local closeFallbackConnection = UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if cleanedUp or not ScreenGui.Parent or not Menu.Visible or ConfirmOverlay.Visible then
		return
	end
	if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then
		return
	end
	if isPointInside(CloseButton, input.Position) then
		requestCloseFromButton()
	end
end)
registerCleanup(function()
	closeFallbackConnection:Disconnect()
end)

local function toggleMenu()
	if not ScreenGui.Parent then
		return
	end
	if Menu.Visible then
		hideMenu()
	else
		showMenu()
	end
end

local launcherDragging = false
local launcherDragInput = nil
local launcherDragStart = nil
local launcherStartPos = nil
local launcherDidDrag = false

local function clampLauncher(position)
	local viewport = getViewport()
	local x = math.clamp(position.X.Scale, 0.0, 0.925)
	local y = math.clamp(position.Y.Scale, 0.02, 0.92)

	if position.X.Scale < 0 then
		x = 0.015
	elseif position.X.Scale > 0.925 then
		x = 0.907
	end

	return UDim2.fromScale(x, y)
end

local function updateLauncherDrag(input)
	if visualPanelOpen then
		return
	end
	if lockEnabled and lockButtonSelected then
		return
	end
	local delta = input.Position - launcherDragStart
	if delta.Magnitude > 4 then
		launcherDidDrag = true
	end
	local viewport = getViewport()
	if viewport.X <= 0 or viewport.Y <= 0 then
		return
	end
	local dx = delta.X / viewport.X
	local dy = delta.Y / viewport.Y
	local target = UDim2.fromScale(
		launcherStartPos.X.Scale + dx,
		launcherStartPos.Y.Scale + dy
	)
	LauncherFrame.Position = clampLauncher(target)
end

local function beginLauncherDrag(input)
	if visualPanelOpen then
		return
	end
	if lockEnabled and lockButtonSelected then
		return
	end
	launcherDragging = true
	launcherDidDrag = false
	launcherDragStart = input.Position
	launcherStartPos = LauncherFrame.Position
	launcherDragInput = input.UserInputType == Enum.UserInputType.Touch and input or nil
end

local function endLauncherDrag()
	launcherDragging = false
	launcherDragInput = nil
	launcherDragStart = nil
	launcherStartPos = nil
end

Launcher.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		beginLauncherDrag(input)
	end
end)

local menuDragging = false
local menuDragInput = nil
local menuDragStart = nil
local menuStartPos = nil
local dragCorner = ""
local pointerDownAt = 0
local didDrag = false

local CornerSize = UDim2.fromScale(0.075, 0.11)
local corners = {}

local function makeCorner(name, position, anchor)
	local corner = make("TextButton", {
		Name = name,
		AnchorPoint = anchor,
		Position = position,
		Size = CornerSize,
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		Text = "",
		AutoButtonColor = false,
		Active = true,
		ZIndex = 10,
	}, Menu)
	return corner
end

corners.TopLeft = makeCorner("DragTopLeft", UDim2.fromScale(0, 0), Vector2.new(0, 0))
corners.TopRight = makeCorner("DragTopRight", UDim2.fromScale(1, 0), Vector2.new(1, 0))
corners.BottomLeft = makeCorner("DragBottomLeft", UDim2.fromScale(0, 1), Vector2.new(0, 1))
corners.BottomRight = makeCorner("DragBottomRight", UDim2.fromScale(1, 1), Vector2.new(1, 1))

for name, corner in pairs(corners) do
	corner.MouseEnter:Connect(function()
		tween(MenuStroke, 0.10, {Color = COLORS.White, Transparency = 0.05})
	end)
	corner.MouseLeave:Connect(function()
		if not menuDragging then
			tween(MenuStroke, 0.10, {Color = COLORS.Gray3, Transparency = 0.12})
		end
	end)
	corner.InputBegan:Connect(function(input)
		if visualPanelOpen then
			return
		end
		if lockEnabled and lockMenuSelected then
			return
		end
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			pointerDownAt = os.clock()
			didDrag = false
			menuDragging = false
			menuDragInput = input.UserInputType == Enum.UserInputType.Touch and input or nil
			menuDragStart = input.Position
			menuStartPos = Menu.Position
			dragCorner = name
		end
	end)
end

function updateMenuDrag(input)
	if visualPanelOpen then
		return
	end
	if lockEnabled and lockMenuSelected then
		return
	end
	if not menuDragStart or not menuStartPos then
		return
	end
	local delta = input.Position - menuDragStart
	if delta.Magnitude < 4 then
		return
	end
	if os.clock() - pointerDownAt < 0.06 then
		return
	end
	didDrag = true
	menuDragging = true

	local viewport = getViewport()
	if viewport.X <= 0 or viewport.Y <= 0 then
		return
	end
	local dx = delta.X / viewport.X
	local dy = delta.Y / viewport.Y
	local x = menuStartPos.X.Scale + dx
	local y = menuStartPos.Y.Scale + dy
	Menu.Position = UDim2.fromScale(x, y)
end

function endMenuDrag()
	if not menuDragging then
		menuDragInput = nil
		return
	end

	local viewport = getViewport()
	local absolutePosition = Menu.AbsolutePosition
	local absoluteSize = Menu.AbsoluteSize
	local fullyLeft = absolutePosition.X + absoluteSize.X < 0
	local fullyRight = absolutePosition.X > viewport.X
	local fullyTop = absolutePosition.Y + absoluteSize.Y < 0
	local fullyBottom = absolutePosition.Y > viewport.Y

	if fullyLeft then
		local y = math.clamp(Menu.Position.Y.Scale, 0.05, 0.95)
		tween(Menu, 0.20, {Position = UDim2.fromScale(0.02, y)})
	elseif fullyRight then
		local y = math.clamp(Menu.Position.Y.Scale, 0.05, 0.95)
		tween(Menu, 0.20, {Position = UDim2.fromScale(0.98, y)})
	elseif fullyTop then
		tween(Menu, 0.20, {Position = UDim2.fromScale(math.clamp(Menu.Position.X.Scale, 0.02, 0.98), 0.05)})
	elseif fullyBottom then
		tween(Menu, 0.20, {Position = UDim2.fromScale(math.clamp(Menu.Position.X.Scale, 0.02, 0.98), 0.95)})
	end

	menuDragging = false
	menuDragInput = nil
	menuDragStart = nil
	menuStartPos = nil
	tween(MenuStroke, 0.12, {Color = COLORS.Gray3, Transparency = 0.12})
end

for name, corner in pairs(corners) do
	corner.Activated:Connect(function()
		if didDrag then
			didDrag = false
			return
		end
	end)
end

local inputChangedConnection = UserInputService.InputChanged:Connect(function(input)
	if launcherDragging and ((launcherDragInput and input == launcherDragInput) or (not launcherDragInput and input.UserInputType == Enum.UserInputType.MouseMovement)) then
		updateLauncherDrag(input)
	end
	if menuDragInput and input == menuDragInput then
		updateMenuDrag(input)
	elseif menuDragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		updateMenuDrag(input)
	elseif menuDragStart and not menuDragInput and input.UserInputType == Enum.UserInputType.MouseMovement then
		updateMenuDrag(input)
	end
end)

local inputEndedConnection = UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		if launcherDragging then
			endLauncherDrag()
		end
		if menuDragging then
			endMenuDrag()
		end
	end
end)

Launcher.Activated:Connect(function()
	if launcherDidDrag then
		launcherDidDrag = false
		return
	end
	toggleMenu()
end)

local function updateSearchVisual()
	local hasText = SearchBox.Text ~= ""
	SearchHint.Visible = not hasText
	SearchClear.Visible = hasText
end

local searchTextConnection = SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
	updateSearchVisual()
	renderResults()
end)

SearchClear.Activated:Connect(function()
	SearchBox.Text = ""
	SearchBox:CaptureFocus()
end)

applyEnglishOnly(ScreenGui)

local englishGuardConnection = ScreenGui.DescendantAdded:Connect(function(descendant)
	applyEnglishOnly(descendant)
end)
registerCleanup(function()
	if englishGuardConnection then
		englishGuardConnection:Disconnect()
	end
end)

setTab("Main")
Menu.Visible = false

local renderSteppedConnection = RunService.RenderStepped:Connect(function()
	if not ScreenGui.Parent then
		return
	end
	updateGlobalScale()
end)

registerCleanup(function()
	for _, connection in pairs(toggleConnections) do
		if connection and connection.Disconnect then connection:Disconnect() end
	end
end)

registerCleanup(function()
	for _, connection in pairs(toggleConnections) do
		pcall(function()
			connection:Disconnect()
		end)
	end
	toggleConnections = {}
	if resultsScrollWheelConnection then
		resultsScrollWheelConnection:Disconnect()
	end
	if resultsScrollInputConnection then
		resultsScrollInputConnection:Disconnect()
	end
	inputChangedConnection:Disconnect()
	inputEndedConnection:Disconnect()
	searchTextConnection:Disconnect()
	renderSteppedConnection:Disconnect()
end)
